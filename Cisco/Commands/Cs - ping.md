---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🛜 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ➊ User >
📝 Description: Проверка сетевой доступности узла (ICMP)
🗂️ Category:
  - 🩺 Diag
⚡ Alias: ping
💻 Command: ping
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `ping` отправляет запросы **ICMP Echo** удаленному узлу, чтобы проверить, доступен ли он по сети.
> * Это главный инструмент сетевого инженера для проверки связи.
> * *Аналог:* «Эхо» в горах (крикнул — услышал ответ).

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Протокол** | `ICMP` (Internet Control Message Protocol) |
> | **Режим ввода** | `User >` или `Privileged #` |
> | **Extended Ping** | Только в режиме `Privileged #` |
> | **Размер пакета** | 100 байт (по умолчанию в Cisco) |

> [!NOTE]- 💻 2. Синтаксис
>
> **1. Быстрый (Simple):**
> ```bash
> ping <destination>
> ```
>
> **2. Расширенный (Extended):**
> Вводится просто `ping` без аргументов (запускает диалог-опросник).
> ```bash
> ping
> ```

> [!NOTE]- ⚙️ 3. Аргументы
> | Аргумент | Тип | Описание |
> | :--- | :---: | :--- |
> | `destination` | `IP/Host` | IP-адрес (напр. `192.168.1.1`) или имя хоста. |
> | `repeat` | `int` | *(Опц)* Количество пакетов (по умолчанию 5). |
> | `size` | `int` | *(Опц)* Размер пакета в байтах. |
> | `source` | `IP/Int` | *(Опц)* От какого интерфейса слать запрос. |

> [!NOTE]- 📝 4. Руководство (Расшифровка символов)
> В отличие от Windows/Linux, Cisco рисует график символами:
>
> 1.  ❗ **`!` (Восклицательный знак)** — **Успех.** Ответ получен (Receipt).
> 2.  💀 **`.` (Точка)** — **Тайм-аут.** Сервер не ответил за 2 секунды.
> 3.  🚫 **`U` (Unreachable)** — **Недоступно.** Маршрутизатор не знает, куда слать пакет (нет маршрута).
>
> **Нормальная картина:** `!!!!!` (5 из 5 успешно).
> **Потеря пакетов:** `!!.!!` (Нестабильная связь).
> **Первый пакет:** Часто первый пакет теряется из-за ARP (`.!!!!`) — это норма.

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Стандартный пинг
> > Отправляет 5 пакетов по 100 байт.
> > ```bash
> > Router# ping 8.8.8.8
> >
> > Type escape sequence to abort.
> > Sending 5, 100-byte ICMP Echos to 8.8.8.8, timeout is 2 seconds:
> > !!!!!
> > Success rate is 100 percent (5/5), round-trip min/avg/max = 1/2/4 ms
> > ```
>
> > [!EXAMPLE]- Расширенный пинг (Extended)
> > Используется, чтобы проверить связь от конкретного интерфейса/VLAN (Source Ping).
> > ```bash
> > Router# ping
> > Protocol [ip]:
> > Target IP address: 10.1.1.1
> > Repeat count [5]: 100
> > Datagram size [100]:
> > Timeout in seconds [2]:
> > Extended commands [n]: y
> > Source address or interface: 192.168.1.1
> > ```

---
