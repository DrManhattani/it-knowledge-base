---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ➋ Priv #
🗂️ Category:
  - ⚙️ Sys
📝 Description: Быстрое сохранение конфигурации (Shorthand)
⚡ Alias: wr
💻 Command: write
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `write` (или сокращенно `wr`) — это самый быстрый способ **сохранить конфигурацию**.
> * Она копирует данные из оперативной памяти (Running-Config) в постоянную память (Startup-Config).
> * **Важно:** Это "старая школа". В официальных экзаменах требуют использовать длинную версию: `copy running-config startup-config`. Но в жизни все пишут `wr`.

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `Router#` (Privileged Exec) |
> | **Сокращение** | `wr` |
> | **Что делает** | Перезаписывает файл сохранения (NVRAM). |
> | **Официальный аналог** | `copy running-config startup-config` |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> write [memory | erase | terminal]
> ```
> *Обычно используется просто слово `write` или `wr`.*

> [!NOTE]- ⚙️ 3. Аргументы
> | Аргумент | Описание |
> | :--- | :--- |
> | `memory` | (По умолчанию) Сохранить в память. То же самое, что просто `wr`. |
> | `erase` | **ОПАСНО.** Удалить конфигурацию запуска (сброс роутера в заводские). Аналог `erase startup-config`. |
> | `terminal` | Показать конфиг на экран (аналог `show run`). Почти не используется. |

> [!NOTE]- 🚀 4. Примеры использования
>
> > [!EXAMPLE]+ Мгновенное сохранение
> > Вы настроили интерфейсы и хотите сохраниться.
> > ```bash
> > Router# wr
> > Building configuration...
> > [OK]
> > Router#
> > ```
>
> > [!EXAMPLE]- Сохранение из режима конфига (через DO)
> > ```bash
> > Router(config)# do wr
> > Building configuration...
> > [OK]
> > ```

---

## 🔗 Связи
* **Что сохраняем:** [[Cs - show running-config | show running-config]]
* **Официальная команда:** [[Cs - copy run start]]