---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw (L3)
📶 Mode:
  - ⚙️ Config
  - ⚙️ Config-Router
🗂️ Category:
  - 🛣️ Routing
📝 Description: Детальный разбор команд конфигурации RIPv2. Технические определения, алгоритмы работы демона и логика фильтрации.
⚡ Alias: ro rip
💻 Command: router rip
---
# 📖 Глава: Протокол RIP (Architecture & Config)

**RIP (Routing Information Protocol)** — это классический дистанционно-векторный протокол (Distance Vector).
Он работает по принципу слухов: *"Я слышал от соседа, что сеть там"*. Роутер не знает полной карты сети, он видит только соседей.

---

## 0. Теоретический паспорт (Theory)

Прежде чем настраивать, нужно знать ограничения протокола.

| Параметр | Значение | Описание |
| :--- | :--- | :--- |
| **Administrative Distance (AD)** | **120** | Рейтинг доверия. RIP менее надежен, чем OSPF (110) и EIGRP (90), но надежнее внешних источников (170). |
| **Метрика** | **Hop Count** (Число прыжков) | Единственный критерий выбора пути — количество роутеров. Скорость линка (10G или 10Mbit) игнорируется. |
| **Лимит пути** | **15 Hops** | 16-й прыжок считается "бесконечностью" (Unreachable). Протокол не работает в сетях диаметром >15 роутеров. |
| **Update Timer** | **30 сек** | Как часто роутер рассылает таблицу маршрутизации соседям. |
| **Convergence** | **Медленная** | Время сходимости (пока вся сеть узнает об изменениях) очень большое из-за долгих таймеров. |
| **Load Balancing** | **4 пути** (Default) | Если есть 4 пути с одинаковым кол-вом хопов, RIP шлет трафик по всем сразу. |

---

## 1. Команда `router rip`

> [!INFO] Определение
> Глобальная команда инициализации процесса. В отличие от OSPF или EIGRP, RIP **не требует** указания номера процесса или автономной системы (AS).

**Технические особенности процесса:**
1.  **Single Instance:** На одном роутере может быть запущен **только один** процесс RIP. Вы не можете создать `router rip 1` и `router rip 2`.
2.  **UDP 520:** Процесс захватывает этот порт. Все коммуникации идут по L4 (Транспортный уровень), в отличие от OSPF (работает поверх IP) или IS-IS (работает поверх L2).
3.  **Local Database:** В памяти создается база данных маршрутов RIP. Маршруты попадают в главную таблицу (`show ip route`) только если у них лучший (меньший) AD, чем у конкурентов.

---

## 2. Команда `version 2`

> [!INFO] Определение
> Переключает движок формирования пакетов. Де-факто стандарт для любых внедрений RIP.

**Сравнительная характеристика механизмов:**

| Параметр | RIP Version 1 (Legacy) | RIP Version 2 (Standard) |
| :--- | :--- | :--- |
| **Структура обновления** | Передает только IP сети. Маска **не передается**. | Передает пару: **IP-адрес + Маска подсети**. |
| **Поддержка VLSM** | Нет. Принимающая сторона сама додумывает маску по классу (A/B/C). | **Да.** Корректно передает /30, /27 и т.д. |
| **Тип рассылки** | **Broadcast** (`255.255.255.255`). Слышат все в сегменте. | **Multicast** (`224.0.0.9`). Слышат только роутеры. |
| **Аутентификация** | Нет. | Поддерживается (MD5 / Plain Text). |

**Вердикт:** Всегда включаем `version 2`.

---

## 3. Команда `no auto-summary`

> [!INFO] Определение
> Отключение автоматического "округления" масок.

**Проблема (Auto-summary включен):**
При передаче маршрута через границу классов (например, подсеть `172.16.1.0/24` проходит через сеть `10.0.0.0`), роутер агрегирует её до дефолтного класса (`172.16.0.0/16`).

**Риск (Discontiguous Networks):**
Если у вас есть офис "А" (`172.16.1.0`) и офис "Б" (`172.16.2.0`), разделенные интернетом, с включенным auto-summary оба офиса будут анонсировать одну и ту же сеть `172.16.0.0/16`. Маршрутизация сломается.

**Результат команды:**
Роутер передает префикс "как есть": `172.16.1.0/24`.

---

## 4. Команда `network <Classful_IP>`

> [!INFO] Определение
> Команда **селектор интерфейсов**. Она не создает маршрут вручную, а ищет интерфейсы для участия в процессе.

**Алгоритм работы:**
1.  **Match:** IOS ищет интерфейсы, чьи IP попадают в указанный диапазон.
2.  **Action 1 (Hello):** Начинает слать обновления в этот порт.
3.  **Action 2 (Advertise):** Добавляет подсеть порта в базу RIP для рекламы другим.

> [!WARNING] Нюанс синтаксиса
> Команда принимает только классовый адрес (Major Network).
> * `network 192.168.1.1` -> превратится в `network 192.168.1.0`
> * `network 10.55.55.1` -> превратится в `network 10.0.0.0`

---

## 5. Команда `passive-interface <Interface_ID>`

> [!INFO] Определение
> Режим "только слушать, но не говорить" в конкретный порт.

**Разделение логики:**
1.  **Transport (Транспорт):** ⛔ **BLOCKED.** Роутер перестает отправлять RIP-пакеты в этот порт.
2.  **Advertise (Реклама):** ✅ **ACTIVE.** Роутер продолжает рассказывать *другим* соседям, что эта сеть существует.

**Применение:**
* **LAN (Users):** Всегда **Passive** (Безопасность + чистота эфира).
* **WAN (Routers):** Всегда **Active** (Иначе разорвется соседство).

---

## 🚀 Итоговый пример (Best Practice)

**Сценарий:**
* `Gi0/0` — LAN (`192.168.10.0/24`).
* `Gi0/1` — WAN (`10.0.0.0/30`).

```bash
Router(config)# router rip                ! 1. Запуск процесса (UDP 520)
Router(config-router)# version 2          ! 2. Вкл. VLSM и Multicast
Router(config-router)# no auto-summary    ! 3. Откл. склеивание подсетей

! 4. Активация интерфейсов (Включаем порты в работу)
Router(config-router)# network 192.168.10.0  ! Активация Gi0/0
Router(config-router)# network 10.0.0.0      ! Активация Gi0/1

! 5. Настройка безопасности
! Блокируем рассылку служебных пакетов пользователям
Router(config-router)# passive-interface GigabitEthernet0/0
```
## 🔗 Связи

- **Верификация:** [[Cs - show ip protocols]] (Показывает таймеры, фильтры и пассивные интерфейсы).
- **Таблица:** [[Cs - show ip route | show ip route]] (Маршруты RIP помечены кодом **R**).
- **Отладка:** `debug ip rip` (Просмотр пакетов в реальном времени).