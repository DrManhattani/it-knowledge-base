---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ➋ Priv #
🗂️ Category:
  - ⚙️ Sys
📝 Description: Просмотр текущей активной конфигурации (RAM)
⚡ Alias: sh run
💻 Command: show running-config
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `show running-config` выводит на экран **текущую активную конфигурацию** устройства.
> * Это то, что находится в **оперативной памяти (RAM)** прямо сейчас.
> * Если выключить питание, этот файл **исчезнет**, если его предварительно не сохранить (командой `write` или `copy`).

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `Router#` (Privileged Exec) |
> | **Сокращение** | `sh run` |
> | **Источник данных** | RAM (Volatile Memory) |
> | **Отличие от startup** | `startup` — это то, что загрузится при старте. `running` — то, что работает сейчас. |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> show running-config [view full] [ | {begin | include | section} <WORD> ]
> ```

> [!NOTE]- 🔍 3. Фильтрация (Pipe |) — Режим PRO
> Выводить весь конфиг (он может быть на 1000 строк) долго. Используйте "трубу" `|`, чтобы найти нужное.
> 
> | Фильтр | Описание | Пример |
> | :--- | :--- | :--- |
> | `| section` | Показать целый блок настроек (самое полезное!). | `sh run \| section ospf` (Покажет только настройки OSPF). |
> | `| include` | Показать только строчки, содержащие слово. | `sh run \| include username` (Покажет только созданных юзеров). |
> | `| begin` | Начать вывод с определенного слова и показать всё дальше. | `sh run \| begin interface` |

> [!fail] ⚠️ 4. Безопасность (Пароли)
> Именно в выводе `show run` видны пароли.
> * Если вы ввели `username ... password 123` (без secret), здесь будет видно слово `123`.
> * Если вы ввели `secret` или включили шифрование, здесь будут видны хэши (`$1$mERr...`).
> * **Совет:** Будьте осторожны, показывая `sh run` коллегам или на стриме, если шифрование не настроено.

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Базовый просмотр
> > ```bash
> > Router# show run
> > ```
> > Листать пробелом (Space), выход на `q`.
>
> > [!EXAMPLE]- Посмотреть только настройки линии (Telnet/Console)
> > Используем фильтр `section`, чтобы не листать весь конфиг.
> > ```bash
> > Router# show run | section line
> > ```
>
> > [!EXAMPLE]- Проверка через DO из режима конфига
> > Вы настраиваете интерфейс и хотите вспомнить IP-адрес другого порта.
> > ```bash
> > Router(config-if)# do sh run
> > ```

---

## 🔗 Связи
* **Что сохраняем:** [[Cs - copy run start]] или [[Cs - write]] (превращает `running` в `startup`)
* **Где лежит сохраненное:** [[show startup-config]] (то, что в NVRAM)
* **Как скрыть пароли:** [[Cs - service password-encryption]]