---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw (L3)
📶 Mode:
  - ⚡ Exec (Privileged)
🗂️ Category:
  - 🔎 Monitoring
  - 🕵️ Troubleshooting
📝 Description: "Выводит детальную информацию об OSPF на интерфейсах: таймеры (Hello/Dead), тип сети, стоимость (Cost), состояние (DR/BDR) и принадлежность к зоне. Главная команда для диагностики соседства."
⚡ Alias: sh ip ospf int
💻 Command: show ip ospf interface [interface-id]
---
# 📖 Глава: Диагностика интерфейсов OSPF

Команда `show ip ospf interface` — это **рентген** для OSPF.
Если `show ip ospf neighbor` показывает, *с кем* мы дружим, то эта команда показывает, *как* мы настроены дружить.
Она критически важна для поиска причин, почему соседство **не** поднимается (Timer mismatch, Area mismatch, Network Type mismatch).

---

> [!INFO]+ 🏗️ 1. Ключевые параметры вывода
> Команда выводит много текста. Вот таблица того, на что нужно смотреть **в первую очередь**:
> 
> | Параметр в выводе | Значение | Что проверять (Troubleshooting) |
> | :--- | :--- | :--- |
> | **Area** | Номер зоны | Должен совпадать с зоной соседа. |
> | **Process ID** | ID процесса | Локальный номер (не обязан совпадать с соседом). |
> | **Router ID** | RID этого роутера | Должен быть уникальным. |
> | **Network Type** | BROADCAST / P2P | **Критично.** Должен совпадать с типом на соседнем роутере. |
> | **Cost** | Стоимость линка | Влияет на выбор маршрута. |
> | **State** | DR / BDR / DROTHER | Роль роутера на этом линке. |
> | **Hello / Dead** | Таймеры (сек) | **Критично.** Обязаны совпадать с соседом (по дефолту 10/40 или 30/120). |

---

> [!EXAMPLE]+ 📟 2. Пример реального вывода (с разбором)
> ```bash
> R1# show ip ospf interface GigabitEthernet0/1
> ```
> 
> **Вывод консоли:**
> ```text
> GigabitEthernet0/1 is up, line protocol is up 
>   Internet Address 10.0.0.1/30, Area 0 
>   Process ID 1, Router ID 1.1.1.1, Network Type BROADCAST, Cost: 1
>   Transmit Delay is 1 sec, State DR, Priority 1
>   Designated Router (ID) 1.1.1.1, Interface address 10.0.0.1
>   Backup Designated router (ID) 2.2.2.2, Interface address 10.0.0.2
>   Timer intervals configured, Hello 10, Dead 40, Wait 40, Retransmit 5
>     Hello due in 00:00:04
>   Index 1/1, flood queue length 0
>   Next 0x0(0)/0x0(0)
>   Last flood scan length is 1, maximum is 1
>   Last flood scan time is 0 msec, maximum is 0 msec
>   Neighbor Count is 1, Adjacent neighbor count is 1 
>     Adjacent with neighbor 2.2.2.2  (Backup Designated Router)
>   Suppress hello for 0 neighbor(s)
> ```
> 
> **🔍 Анализ:**
> 1.  **Интерфейс:** `GigabitEthernet0/1` (UP/UP).
> 2.  **Зона:** `Area 0`.
> 3.  **Тип сети:** `BROADCAST` (значит, будут выборы DR/BDR).
> 4.  **Роль:** `State DR` (Этот роутер — главный в сегменте).
> 5.  **Таймеры:** `Hello 10, Dead 40`. Если у соседа будет 11/40 — связь не поднимется.
> 6.  **Соседи:** `Neighbor Count is 1` (Видим одного соседа).

---

> [!TIP]+ 🚀 3. Полезная вариация: "Brief"
> Если вам не нужны подробности, а нужно просто увидеть список всех интерфейсов, где включен OSPF, используйте модификатор `brief`. Это лучшая команда для быстрой инвентаризации.
> 
> ```bash
> R1# show ip ospf interface brief
> 
> Interface    PID   Area            IP Address/Mask    Cost  State  Nbrs F/C
> Gi0/1        1     0               10.0.0.1/30        1     DR     1/1
> Lo0          1     0               1.1.1.1/32         1     LOOP   0/0
> ```
> * **PID:** Process ID.
> * **State:** Роль (DR, BDR, P2P, LOOP).
> * **Nbrs F/C:** Количество соседей (Full / Configured).

---

## 🔗 Связи
* **Настройка:** Включение OSPF [[Cs - router ospf | router ospf]].
* **Соседи:** Посмотреть список соседей [[Cs - show ip ospf neighbor | show ip ospf neighbor]].
* **Типы сетей:** Изменение Network Type [[Cs - ip ospf network | ip ospf network]].