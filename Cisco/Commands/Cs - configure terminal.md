---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - "➋ Priv #"
🗂️ Category:
  - ⚙️ Sys
📝 Description: Вход в режим глобальной конфигурации
⚡ Alias: conf t
💻 Command: configure terminal
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `configure terminal` переводит устройство в режим **Global Configuration**.
> * Это «Мастерская»: здесь вносятся любые изменения, влияющие на устройство целиком.
> * Все изменения применяются **мгновенно**.

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `➋ Priv #` |
> | **Куда попадаем** | `(config)#` (Global) |
> | **Обратная команда** | `exit` или `end` |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> configure terminal
> ```

> [!NOTE]- ⚙️ 3. Аргументы
> | Аргумент | Тип | Описание |
> | :--- | :---: | :--- |
> | `terminal` | `source` | Настройка вручную через текущую консоль. |
> | `memory` | `source` | *(Устар.)* Загрузить из NVRAM. |
> | `network` | `source` | *(Устар.)* Загрузить по TFTP. |

> [!NOTE]- 📝 4. Руководство (Usage Guidelines)
> 1.  ⚡ **Мгновенный эффект:** В Cisco нет кнопки «Применить». Нажал Enter — настройка заработала.
> 2.  💾 **Память:** Вы меняете `running-config` (ОЗУ). Чтобы сохранить навсегда, нужно выйти и прописать `write`.
> 3.  🚪 **Выход:**
>     * `exit` — шаг назад.
>     * `end` (Ctrl+Z) — полный выход в начало.

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Стандартный вход
> > ```bash
> > Router# configure terminal
> > Enter configuration commands, one per line.  End with CNTL/Z.
> > Router(config)#
> > ```
>
> > [!EXAMPLE]- Смена имени хоста
> > ```bash
> > Router# conf t
> > Router(config)# hostname R-Core
> > R-Core(config)# exit
> > ```

---

## 🔗 Связи
* **Предыдущий шаг:** [[Cs - enable | enable]]
* **Полезно внутри:** [[hostname]], [[interface]]