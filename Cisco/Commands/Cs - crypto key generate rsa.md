---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - 🛡️ Sec
📝 Description: Генерация пары ключей RSA (Публичный + Приватный). Запускает процесс SSH-сервера.
⚡ Alias: cry key gen rsa
💻 Command: crypto key generate rsa
---

# 💡 Описание
> [!SUMMARY] Суть
> Команда `crypto key generate rsa` — это "кнопка запуска" криптографического ядра роутера.
> Она инициирует сложный математический процесс создания уникальной пары ключей (Key Pair).
> 
> **Ключевые последствия ввода команды:**
> 1.  Создается **Публичный ключ** (Public Key), который роутер будет отдавать всем клиентам.
> 2.  Создается **Приватный ключ** (Private Key), который остается в секрете внутри чипа.
> 3.  Автоматически включается служба **SSH** (так как она не может работать без ключей).
> 4.  Автоматически включается поддержка **HTTPS** (Secure HTTP server).

---

> [!INFO]+ 🏗️ 1. Теория: Зачем это нужно? (Механика PKI)
> Роутер не может просто так включить шифрование. Ему нужна математическая основа для этого. Cisco использует алгоритм **RSA** (Rivest–Shamir–Adleman).
> 
> **Как это работает в SSH:**
> Когда вы подключаетесь к роутеру через Putty/Terminal:
> 1.  Роутер отправляет вам свой **Публичный ключ**.
> 2.  Ваш компьютер использует этот ключ, чтобы зашифровать "секрет" (сессионный ключ).
> 3.  Только роутер может расшифровать это сообщение, используя свой **Приватный ключ**.
> 
> *Без этой сгенерированной пары, роутеру "нечем" открывать зашифрованные послания, поэтому SSH-порт (22) будет закрыт.*

> [!danger]+ 🧱 2. Жесткие зависимости (Prerequisites)
> Это самая частая причина ошибок. Роутер **откажется** генерировать ключи, если он "Аноним".
> Ключи RSA в экосистеме Cisco жестко привязаны к **FQDN** (Fully Qualified Domain Name).
> 
> **Формула имени ключа:**
> $$Key Label = Hostname + "." + DomainName$$
> 
> **Типичные ошибки:**
> * Если имя устройства `Router` (дефолтное) -> **% Please define a hostname other than Router.**
> * Если не задан `ip domain-name` -> **% Please define a domain name first.**
> 
> **Правильный порядок действий:**
> 1.  `hostname R1`
> 2.  `ip domain-name cisco.lab`
> 3.  `crypto key generate rsa`

> [!example]+ 🛠️ 3. Полный синтаксис и опции (Advanced CLI)
> Обычно команду вводят просто так и нажимают Enter. Но для скриптов и автоматизации полезно знать полные аргументы, чтобы избежать интерактивных вопросов.
> 
> **Шаблон:**
> `crypto key generate rsa [general-keys | usage-keys] [label <NAME>] [modulus <BITS>] [exportable]`
> 
> | Аргумент | Описание | Зачем использовать? |
> | :--- | :--- | :--- |
> | `modulus <360-4096>` | Указывает размер ключа сразу в строке. | **Автоматизация.** Роутер не будет спрашивать "How many bits?". Полезно для скриптов заливки конфига. |
> | `label <NAME>` | Задает уникальное имя ключу вместо стандартного `FQDN`. | **Мульти-аренда.** Если нужно хранить несколько разных ключей на одном роутере (например, для разных VRF или PKI-сервисов). |
> | `exportable` | Разрешает просмотр приватного ключа. | **Резервирование.** Позволяет перенести ключи на другой (запасной) роутер при настройке кластера. |
> | `general-keys` | (По умолчанию). Создает одну пару ключей для всего. | Стандартный режим. Указывать не обязательно. |
> | `usage-keys` | Создает две пары: одну для подписи, другую для шифрования. | Устаревший стандарт. Сейчас почти не используется. |
> 
> **Пример "тихой" генерации (без вопросов):**
> ```bash
> crypto key generate rsa modulus 2048
> ```

> [!NOTE]+ 📏 4. Размер ключа (Modulus Size) — Главный выбор
> Если вы не указали `modulus` в строке, система задаст вопрос:
> `How many bits in the modulus [512]:`
> 
> Это число определяет сложность "замка". Чем больше бит, тем сложнее взломать, но тем больше нагрузка на процессор при подключении.
> 
> | Размер (Бит) | Надежность | Совместимость SSH | Вердикт |
> | :--- | :--- | :--- | :--- |
> | **512** | ❌ **Критически низкая.** Взламывается за часы. | Поддерживает только **SSH v1**. Версия 2 не включится. | ⛔ **Запрещено** |
> | **768** | ⚠️ Низкая. | Минимум для работы **SSH v2**. | Не рекомендуется |
> | **1024** | ✅ Средняя (Lab Standard). | Поддерживает SSH v2. Генерируется быстро. | **Стандарт для учебы** |
> | **2048** | 🛡️ **Высокая (NIST Standard).** | Рекомендовано для Production. Генерируется 10-60 сек. | **Стандарт для работы** |
> | **4096** | 🏰 Параноидальная. | Максимальная защита. Сильно грузит CPU роутера при каждом входе. | Только спец. задачи |
> 
> *Если вы выберете 512, роутер останется уязвимым для Downgrade-атак.*

> [!TIP] 🔄 5. Жизненный цикл ключей (Lifecycle Management)
> Ключи не вечны. Иногда их нужно менять или удалять.
> 
> **А. Перегенерация (Refresh / Upgrade)**
> Если вы хотите увеличить безопасность с 1024 до 2048 бит:
> Просто введите команду `crypto key generate rsa` заново.
> * Роутер спросит: `% You already have RSA keys... Do you really want to replace them? [yes/no]:`
> * Если нажать `yes`, старые ключи удалятся, новые создадутся.
> * ⚠️ **Внимание:** Все активные SSH-сессии мгновенно оборвутся!
> 
> **Б. Полное удаление (Zeroize)**
> Если нужно выключить SSH и стереть следы криптографии (например, перед продажей роутера).
> Команда:
> ```bash
> crypto key zeroize rsa
> ```
> *После этого SSH сервер останавливается (`%SSH-5-DISABLED`).*

> [!NOTE]- 🔐 6. Продвинутый уровень: Экспорт ключей
> По умолчанию ключи создаются как **Non-Exportable**. Это значит, что даже администратор с правами `root` не может скопировать приватный ключ из памяти роутера на флешку (защита от инсайдера).
> 
> Если вы настраиваете отказоустойчивую пару роутеров (HSRP) и хотите, чтобы у них были одинаковые ключи (чтобы при падении основного роутера клиенты не видели ошибку "Key Changed"), нужно использовать флаг `exportable`:
> ```bash
> crypto key generate rsa label MY-HSRP-KEYS exportable
> ```
> *Только тогда ключи можно будет увидеть через `show crypto key mypubkey rsa private` и перенести в конфиг другого устройства.*

---

## 🔗 Связи
* **Необходимые условия:** [[Cs - hostname | hostname]] и [[Cs - ip domain-name | ip domain-name]]
* **Что настраиваем дальше:** После генерации ключей идем в [[Cs - ip ssh | ip ssh]], чтобы отключить v1 и настроить тайминги.