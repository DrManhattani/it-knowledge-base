---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ⌘ Sw (L2/L3)
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - 🔌 Interfaces
  - 🕹️ Mgmt
📝 Description: Создание SVI (Switch Virtual Interface) для управления коммутатором или для L3-маршрутизации (Inter-VLAN).
⚡ Alias: int vlan
💻 Command: interface vlan
---
# 📖 Глава: SVI (Switch Virtual Interface)

На коммутаторах физические порты (Gi0/1) обычно работают на уровне L2 (коммутация) и не имеют IP-адресов.
Чтобы дать коммутатору IP-адрес (для управления или маршрутизации), мы создаем **виртуальный интерфейс**, привязанный к определенному VLAN.

Эта сущность называется **SVI**.

---

> [!INFO]+ 🏗️ 1. Два режима использования
> Команда одна и та же, но цель зависит от типа коммутатора.
> 
> | Тип свитча | Роль интерфейса | Кол-во интерфейсов | Нужна ли маршрутизация? |
> | :--- | :--- | :--- | :--- |
> | **L2 Switch** (2960) | **Management IP.** Только для того, чтобы админ мог зайти по SSH/Telnet. Свитч ведет себя как обычный компьютер. | Обычно **Один** (VLAN управления). | Нет. (Нужен `ip default-gateway`). |
> | **L3 Switch** (3850) | **Gateway.** Свитч работает как роутер, пересылая пакеты между отделами. | **Много** (по одному на каждый VLAN). | Да (`ip routing`). |

> [!danger]+ 🧱 2. Механика Autostate (Почему он Down?)
> SVI — это виртуальный призрак. Он не может существовать без физического тела.
> Часто новички вводят `no shutdown`, но видят статус `down/down`.
> 
> **Чтобы SVI поднялся (стал Up/Up), нужно выполнить 3 условия:**
> 1.  **VLAN существует:** Он должен быть создан в базе (`vlan 10`).
> 2.  **SVI включен:** На самом `interface vlan` нет команды shutdown.
> 3.  **Есть живой порт:** В этом VLAN должен быть хотя бы один физический порт (Access или Trunk) в состоянии `Up`.
> 
> *Логика: Если в VLAN 10 нет ни одного активного линка (все компьютеры выключены), свитч гасит SVI, чтобы не тратить ресурсы CPU.*

> [!example]+ ⚙️ 3. Настройка (Сценарий L2 - Управление)
> Задача: Настроить IP-адрес для управления в VLAN 99 и **сделать его доступным для пинга**.
> 
> **Этап А: Внутренняя логика (Создаем SVI)**
> ```bash
> ! 1. Создаем L2-сущность (Обязательно!)
> vlan 99
>  name MGMT
> 
> ! 2. Создаем SVI (L3-сущность)
> interface vlan 99
>  description Management_Interface
>  ip address 192.168.99.2 255.255.255.0
>  no shutdown
> 
> ! 3. Указываем шлюз (Для ответа в другие сети)
> exit
> ip default-gateway 192.168.99.1
> ```
> 
> **Этап Б: Физическая активация (Поднимаем линк)**
> *Без этого этапа SVI будет лежать!*
> ```bash
> ! Назначаем физический порт в наш VLAN
> interface GigabitEthernet0/1
>  description Link_to_Admin_PC
>  switchport mode access
>  switchport access vlan 99
>  no shutdown
> ```
> *Теперь нужно физически воткнуть кабель в порт Gi0/1. Как только загорится лампочка линка -> SVI станет Up -> Пинг пойдет.*

> [!example]+ 🚀 4. Настройка (Сценарий L3 - Маршрутизация)
> Задача: Свитч должен сам пересылать трафик между VLAN 10 и VLAN 20.
> 
> ```bash
> ! 0. Включаем мозг роутера
> ip routing
> 
> ! 1. Настраиваем шлюз для VLAN 10
> interface vlan 10
>  ip address 192.168.10.1 255.255.255.0
>  no shutdown
> 
> ! 2. Настраиваем шлюз для VLAN 20
> interface vlan 20
>  ip address 192.168.20.1 255.255.255.0
>  no shutdown
> ```

> [!check]+ 🔐 5. Финальный штрих: Включение SSH
> SVI доступен для пинга, но зайти на него нельзя (Connection Refused), пока не настроен SSH.
> 
> **Минимальный набор команд:**
> ```bash
> ! 1. Имя и Домен (Нужны для генерации ключей)
> hostname SW-MAIN
> ip domain-name corporate.lan
> 
> ! 2. Генерация крипто-ключей
> crypto key generate rsa modulus 2048
> 
> ! 3. Создаем админа
> username admin privilege 15 secret cisco123
> 
> ! 4. Открываем виртуальные линии (VTY)
> line vty 0 4
>  login local
>  transport input ssh
> ```

---

## 🔗 Связи
* **Отличие от Роутера:** На роутерах используется `interface gi0/0.10` (Sub-interfaces), а не `interface vlan`.
* **Требование:** Работает только если есть [[Cs - vlan]].
* **Маршрутизация:** Для L3 режима нужна команда [[Cs - ip routing]].
* **Настройка доступа:** Подробнее про порты в [[Cs - switchport access | switchport access]], [[Cs - switchport trunk | switchport trunk]], [[Cs - switchport mode | switchport mode]].