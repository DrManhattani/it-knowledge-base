---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - ⚙️ Sys
  - 🛡️ Sec
📝 Description: "Фундаментальный разбор доменного имени: формирование FQDN, роль в криптографии (SSH/RSA) и работе DNS-клиента."
⚡ Alias: ip dom na
💻 Command: ip domain-name
---
# 📖 Глава: Доменное имя и FQDN

В сетевом мире у каждого устройства есть два имени:
1.  **Короткое имя (Hostname):** Как мы зовем устройство "дома" (в локальной сети). Например: `R1`.
2.  **Полное имя (FQDN - Fully Qualified Domain Name):** Как устройство записано в "паспорте" (в глобальной системе DNS). Например: `R1.cisco.com`.

Команда `ip domain-name` отвечает за вторую часть — за "фамилию" устройства.

---

## 🟢 Уровень 1. Базовая логика (Identity)

### Аналогия с людьми
Представьте, что `hostname` — это ваше Имя (Иван), а `ip domain-name` — это ваша Фамилия (Петров).
* Внутри своей семьи (локально) вы просто "Иван".
* Но для банка или налоговой (для внешнего мира и криптографии) вы — "Иван Петров". Нельзя выдать паспорт просто "Ивану".

### Формирование FQDN
Когда вы вводите две эти команды, IOS автоматически склеивает их через точку.

$$FQDN = Hostname + "." + DomainName$$

**Пример:**
1.  `hostname Core-Router`
2.  `ip domain-name head-office.local`
3.  **Итоговое системное имя:** `Core-Router.head-office.local`

Это итоговое имя становится уникальным идентификатором устройства во всем мире (или во всей корпоративной сети).

---

## 🟡 Уровень 2. Роль в Криптографии и SSH

Это самый частый сценарий, где новички спотыкаются. Почему роутер отказывается включать SSH без доменного имени?

### Генерация ключей RSA
Для работы SSH нужны криптографические ключи (пара Публичный/Приватный).
Алгоритм RSA требует "зерно" (seed) — уникальную строку, на основе которой начнется математическая магия генерации ключей.

Cisco IOS использует **FQDN** в качестве этого зерна.
* Если домена нет, FQDN равно просто `Hostname` (например, `Router`). Это слишком банально и неуникально.
* Старые версии IOS вообще выдают ошибку: `% You must define a hostname and domain name first`.
* Новые версии могут сгенерировать ключи просто по hostname, но будут ругаться предупреждениями.

> **Золотое правило:** Сначала задаем `hostname` и `ip domain-name`, и только потом вводим `crypto key generate rsa`. Ключи привязываются к этому имени. Если вы потом смените домен — ключи станут недействительными (orphaned) и их придется перегенерировать.

---

## 🟠 Уровень 3. Роль в DNS (Клиентская часть)

Роутер Cisco сам может выступать в роли клиента, который ищет другие узлы по именам. Здесь `ip domain-name` работает как **"Суффикс по умолчанию"**.

### Сценарий: PING по имени
Представьте, что в вашей сети есть сервер с полным именем `server01.cisco.lab`.
Вы находитесь на роутере, у которого настроен `ip domain-name cisco.lab`.

1.  Вы пишете команду: `ping server01`
2.  Роутер видит, что в имени нет точек.
3.  Он **автоматически подставляет** свой домен: `server01` + `.cisco.lab`.
4.  Он отправляет запрос к DNS-серверу: "Скажи мне IP адрес `server01.cisco.lab`".

Если бы домен не был задан, вам пришлось бы всегда писать полные имена.

---

## 🔴 Уровень 4. Продвинутые нюансы (Expert Level)

### A. Список доменов (ip domain-list)
Что делать, если у вас в компании несколько доменов? Например, `msk.company.com` и `spb.company.com`.
Команда `ip domain-name` позволяет задать только **один** основной домен.

Для расширенного поиска используется команда:
`ip domain-list <NAME>`

Вы можете добавить несколько суффиксов.
**Пример:**
```bash
ip domain-list msk.company.com
ip domain-list spb.company.com
```

Теперь, если вы напишете `ping server1`, роутер сначала попробует `server1.msk...`, а если не найдет — `server1.spb...`. Это аналог "Search Domains" в настройках Windows/Linux.

### B. DHCP и автоматизация

Если роутер получает IP-адрес от провайдера по DHCP (на интерфейсе стоит `ip address dhcp`), он может получить доменное имя **автоматически** (DHCP Option 15).

- В этом случае ручная команда `ip domain-name` может быть перезаписана или иметь меньший приоритет (зависит от настроек `no ip dhcp client request domain-name`).

### C. VRF Awareness (Виртуальные роутеры)

В больших сетях (MPLS/ISP) один физический роутер разбит на много виртуальных (VRF).

- Команда `ip domain-name` задается **глобально**.
- Однако DNS-запросы (lookup) могут выполняться в контексте конкретного VRF.
- Современные IOS XE позволяют задавать домены специфично для VRF, но в классическом IOS это глобальная настройка, влияющая на генерацию ключей для всего шасси.
## 🧠 Итоговая сводка

|**Команда**|**Контекст**|**Влияние**|
|---|---|---|
|`ip domain-name cisco.com`|**Основная**. Задает "Фамилию".|1. Формирует имя ключей RSA (SSH).<br><br>  <br><br>2. Основной суффикс для DNS-поиска.|
|`ip domain-list cisco.com`|**Дополнительная**. Список поиска.|Только для DNS-поиска (удобство пинга). Не влияет на SSH.|
|`ip domain-lookup`|**Служба**. Вкл/Выкл DNS.|Разрешает роутеру вообще обращаться к DNS-серверу.|
|`ip name-server 8.8.8.8`|**Настройка**. Адрес DNS.|Указывает, КОГО спрашивать про имена.|

Теперь у тебя есть полная картина: 
1. **Для SSH:** Эта команда обязательна как часть формулы генерации ключа. 
2. **Для удобства:** Она работает как "автодополнение" при пингах. 
3. **Для порядка:** Она определяет полное имя устройства в системе мониторинга. Следующий шаг — это, собственно, сама генерация ключей (`crypto key generate rsa`). Там тоже есть о чем поговорить (размер ключа, версии SSH 1.99 vs 2.0).