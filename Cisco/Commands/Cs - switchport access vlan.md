---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config-If
🗂️ Category:
  - 🔌 Interfaces
  - 🕹️ Mgmt
📝 Description: Назначение конкретного VLAN (ID) на порт доступа. Определяет, в какой подсети будет находиться подключенное устройство.
⚡ Alias: sw acc vlan
💻 Command: switchport access vlan
---
# 📖 Глава: Привязка порта к VLAN

Команда `switchport access vlan <ID>` — это основной инструмент сегментации сети.
Она сообщает коммутатору: **"Любой трафик, который входит в этот порт, принадлежит к VLAN <ID>"**.

---

> [!INFO]+ 🏗️ 1. Логика работы
> Входящий кадр от компьютера **не имеет метки** (Untagged).
> Как только кадр попадает внутрь порта, коммутатор "клеит" на него метку VLAN, указанную в этой команде, и отправляет дальше гулять по своей внутренней шине.
> 
> **Синтаксис:**
> ```bash
> Switch(config-if)# switchport access vlan <VLAN_ID>
> ```
> * `<VLAN_ID>`: Число от 1 до 4094.

> [!example]+ ⚙️ 2. Базовая настройка
> Классический пример настройки порта для компьютера сотрудника из отдела HR (VLAN 20).
> 
> ```bash
> interface GigabitEthernet0/1
>  description PC_Manager_HR
>  ! 1. Явно говорим, что это порт доступа
>  switchport mode access
>  
>  ! 2. Назначаем VLAN
>  switchport access vlan 20
>  
>  ! 3. Включаем (хорошая привычка)
>  no shutdown
> ```

> [!TIP] 🪄 3. Магия автосоздания
> В Cisco IOS есть удобная фича для ленивых.
> Если вы привязываете порт к VLAN, которого **еще нет** в базе данных коммутатора, Cisco создаст его автоматически.
> 
> **Пример:**
> ```bash
> Switch(config)# interface gi0/5
> Switch(config-if)# switchport access vlan 50
> % Access VLAN does not exist. Creating vlan 50
> ```
> *VLAN 50 создан, но у него нет имени (будет `VLAN0050`). Лучше зайти и переименовать потом.*

> [!danger]+ 🧱 4. "Спящий" режим (Конфликт с Trunk)
> Это самый частый источник путаницы.
> Команда `switchport access vlan 20` **не делает порт аксессом**. Она только задает параметр.
> 
> **Сценарий ошибки:**
> 1. Порт настроен как `switchport mode trunk`.
> 2. Вы вводите `switchport access vlan 20`.
> 3. **Результат:** Порт продолжает работать как Trunk! Ваша команда принята, но она **игнорируется** (неактивна).
> 
> Она сработает только тогда, когда (и если) вы переведете порт в `mode access`.

> [!NOTE]+ 📞 5. Voice VLAN (Исключение для телефонов)
> Единственный случай, когда порт доступа принимает трафик сразу двух VLAN.
> 
> ```bash
> interface gi0/1
>  switchport mode access
>  switchport access vlan 10   ! DATA (Компьютер)
>  switchport voice vlan 20    ! VOICE (IP-телефон)
> ```
> * **Компьютер** шлет данные без тега -> попадает в VLAN 10.
> * **Телефон** шлет данные с тегом 20 -> попадает в VLAN 20.

---

## 🔗 Связи
* **Главный переключатель:** Работает в паре с [[Cs - switchport mode vs access | switchport mode access]].
* **Проверка:** Смотреть результат через `show vlan brief`.
* **База данных:** Создание VLAN через [[Cs - vlan]].