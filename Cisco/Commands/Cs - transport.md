---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config-Line
🗂️ Category:
  - 🔌 Lines
  - 🛡️ Sec
📝 Description: Управление разрешенными протоколами (SSH/Telnet) на линиях
⚡ Alias: trans in / trans out
💻 Command: transport {input | output}
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `transport` определяет, **какие протоколы** разрешено использовать для соединения с этой линией.
> * Это жесткий фильтр. Если протокол не указан в списке, роутер просто сбросит соединение, даже не спросив пароль.
> * Работает внутри режимов `line vty` (сетевой доступ) и `line console` (иногда).

---

> [!NOTE]+ 🏗️ 1. Направления (Input vs Output)
> В Cisco есть два вектора контроля. Их часто путают.
> 
> | Команда | Направление | Вопрос | Важность |
> | :--- | :--- | :--- | :--- |
> | `transport input` | **Входящие** к роутеру | *"По какому протоколу админу разрешено подключаться **К** этому роутеру?"* | 🔴 **Критически важно.** Именно здесь мы убиваем Telnet. |
> | `transport output` | **Исходящие** от роутера | *"По какому протоколу админ, сидящий НА этом роутере, может подключиться **К** другому устройству?"* | 🟢 **Редко меняют.** Обычно оставляют `all`. |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> transport {input | output} {ssh | telnet | all | none}
> ```
> *Можно указывать несколько протоколов через пробел: `transport input ssh telnet`*

> [!NOTE]- ⚙️ 3. Аргументы (Протоколы)
> | Аргумент | Описание | Вердикт безопасности |
> | :--- | :--- | :--- |
> | `ssh` | Разрешить только зашифрованное соединение. | ✅ **Must Have** |
> | `telnet` | Разрешить открытый текст. | ❌ **Запретить** (кроме глухих лаб) |
> | `all` | Разрешить всё (и Telnet, и SSH, и устаревшие rlogin/lat). | ⚠️ **Опасно** (часто стоит по умолчанию) |
> | `none` | Запретить всё. Полная изоляция линии. | 🛡️ Используется для лишних линий (например `vty 5 15`). |

> [!fail] 🛡️ 4. Ловушка "По умолчанию"
> На разных версиях IOS поведение по умолчанию разное:
> * **Старые IOS:** `transport input all` (Telnet открыт!).
> * **Новые IOS:** Часто `transport input none` (пока не настроишь — не войдешь).
> 
> **Золотое правило:** Всегда прописывай `transport input ssh` явно. Не надейся на дефолт.

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Стандарт защиты (Только SSH)
> > Мы говорим: "На эти линии можно зайти ТОЛЬКО по SSH. Любые попытки Telnet — отбивать".
> > ```bash
> > Router(config)# line vty 0 4
> > Router(config-line)# transport input ssh
> > ```
>
> > [!EXAMPLE]- Режим "Прыжка" (Jump Host)
> > Допустим, вы зашли на этот роутер и хотите с него прыгнуть на соседний.
> > Команда ниже запрещает вам использовать дырявый Telnet для исходящих подключений с этого устройства.
> > ```bash
> > Router(config-line)# transport output ssh
> > ```
> > *Теперь, если вы напишете в консоли `telnet 192.168.1.2`, роутер скажет "Forbidden".*

---

## 🔗 Связи
* **Где применяем:** [[Cs - line vty | line vty]]
* **Что нужно для работы `input ssh`:** Сгенерированные ключи (тема следующего урока)