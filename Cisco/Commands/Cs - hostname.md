---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - ⚙️ Sys
📝 Description: Изменение сетевого имени устройства (Identity)
⚡ Alias: host
💻 Command: hostname
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `hostname` задает уникальное имя устройству.
> * Это имя сразу отображается в **приглашении командной строки** (вместо `Router(config)#` становится `R1(config)#`).
> * Это **обязательное условие** для генерации крипто-ключей (SSH).
> * Используется соседями (по протоколам CDP/LLDP) для идентификации этого устройства.

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `(config)#` (Global Configuration) |
> | **По умолчанию** | `Router` (или `Switch`) |
> | **Ограничения** | Без пробелов (используйте дефис `-` или подчеркивание `_`). |
> | **Важность** | **Критическая.** Нельзя оставлять имя по умолчанию в продакшне. |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> hostname <НОВОЕ_ИМЯ>
> ```
> *Вернуть имя по умолчанию:*
> ```bash
> no hostname
> ```

> [!NOTE]- ⚙️ 3. Стандарты именования (Naming Convention)
> В серьезных сетях имена придумывают не на ходу. Обычно используют формулу:
> **ГДЕ-КТО-НОМЕР**
> 
> * `MSK-RTR-01` (Москва, Роутер, 1-й)
> * `SPB-SW-MAIN` (Питер, Свитч, Главный)
> * `LAB-R1` (Лаборатория, Роутер 1)

> [!fail] 🔐 4. Связь с SSH (Почему это важно?)
> Если вы попытаетесь сгенерировать ключи для SSH на устройстве с именем `Router`, вы получите ошибку (на старых IOS) или предупреждение.
> Алгоритм RSA использует пару `hostname` + `domain-name` для создания уникальной подписи.
> **Нет уникального имени — нет ключей — нет SSH.**

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Смена имени
> > Обратите внимание, как меняется приглашение (prompt) сразу после нажатия Enter.
> > ```bash
> > Router(config)# hostname R1
> > R1(config)#
> > ```
>
> > [!EXAMPLE]- Сброс имени
> > ```bash
> > R1(config)# no hostname
> > Router(config)#
> > ```

---

## 🔗 Связи
* **Необходима для:** [[Cs - crypto key generate rsa]] (Генерация ключей)
* **Работает в паре с:** [[Cs - ip domain-name]] (Вместе они создают FQDN)