---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - ⚙️ Sys
📝 Description: Отключение DNS-поиска (Защита от зависания при опечатках)
⚡ Alias: no ip dom lo
💻 Command: no ip domain-lookup
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `no ip domain-lookup` запрещает роутеру воспринимать неизвестные команды как **имена сайтов**.
> * **Проблема:** По умолчанию, если вы ошиблись и написали `pign` вместо `ping`, роутер думает: "Хм, это не команда... Наверное, это имя сервера `pign.com`!" и начинает ломиться в DNS-сервер, чтобы узнать его IP.
> * **Результат:** Консоль "зависает" на 30–60 секунд, пока роутер пытается найти этот несуществующий сервер.
> * **Решение:** Эта команда отключает такое поведение. Опечатка сразу выдает ошибку, не блокируя работу.

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `(config)#` |
> | **По умолчанию** | Включено (`ip domain-lookup`) |
> | **Зачем нужно в Лабе** | **Обязательно.** Чтобы не ждать минуту каждый раз, когда промахнулись по клавише. |
> | **Зачем нужно в Жизни** | Часто отключают, если роутеру не нужно выходить в интернет по доменным именам. |

> [!NOTE]- 💻 2. Синтаксис
> **Отключить (Рекомендуется):**
> ```bash
> no ip domain-lookup
> ```
> 
> **Включить обратно (По умолчанию):**
> ```bash
> ip domain-lookup
> ```

> [!fail] 😡 3. Как выглядит "Боль" (Если не отключить)
> Вы случайно вводите ерунду (`asdf`) в режиме `#`:
> ```bash
> Router# asdf
> Translating "asdf"...domain server (255.255.255.255)
> % Unknown command or computer name, or unable to find computer address
> ```
> *Пока висит надпись `Translating...`, вы не можете ничего нажать. `Ctrl+C` часто не помогает (нужно жать `Ctrl+Shift+6`).*

> [!NOTE]- 🚀 4. Примеры использования
>
> > [!EXAMPLE]+ Стандартная настройка "анти-стресс"
> > ```bash
> > Router(config)# no ip domain-lookup
> > ```
> > Теперь при ошибке:
> > ```bash
> > Router# asdf
> > Translating "asdf"... % Unknown command...
> > ```
> > *(Ошибка вылетает мгновенно, можно работать дальше).*

---

## 🔗 Связи
* **Полезно вместе с:** [[logging synchronous]] (еще одна команда для комфорта)
* **Горячая клавиша спасения:** Если забыли отключить и зависли — жмите **`Ctrl` + `Shift` + `6`**, чтобы прервать поиск.