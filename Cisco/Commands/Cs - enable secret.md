---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - 🛡️ Sec
📝 Description: Установка пароля для привилегированного режима (MD5 Hash)
⚡ Alias: en sec
💻 Command: enable secret
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `enable secret` устанавливает пароль, который запрашивается при вводе команды `enable` (переход из режима `>` в режим `#`).
> * Это **"Пароль от режима Бога"**.
> * Он един для всего устройства (не зависит от имени пользователя).
> * Пароль хранится в виде **хэша MD5** (Type 5), что делает его очень надежным.

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `(config)#` (Global Configuration) |
> | **Кого защищает** | Вход в привилегированный режим (Privileged Exec). |
> | **Тип шифрования** | **Type 5** (MD5). Необратимый хэш. |
> | **Нужна ли service password-encryption?** | **Нет.** Этот пароль шифруется сам автоматически. |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> enable secret <ПАРОЛЬ>
> ```
> *Можно также указать уровень прав (необязательно): `enable secret level 15 <PASS>`*

> [!fail] ⚔️ 3. Важно: Secret vs Password
> В Cisco есть две команды для одной задачи.
> 
> 1.  `enable password` (Устарело):**
>     * Хранит пароль текстом или слабым шифром (Type 7).
>     * Взламывается мгновенно.
>     * **Не использовать.**
> 2.  `enable secret` (Стандарт):**
>     * Хранит MD5 хэш.
>     * **Использовать всегда.**
> 
> **Приоритет:** Если вы по ошибке ввели обе команды, роутер **всегда выбирает `secret` и игнорирует `password`.

> [!NOTE]- 🚀 4. Примеры использования
>
> > [!EXAMPLE]+ Стандартная настройка
> > Устанавливаем пароль `class` для админского доступа.
> > ```bash
> > Router(config)# enable secret class
> > ```
> > *Теперь при вводе `en` роутер спросит пароль.*
>
> > [!EXAMPLE]- Смена пароля
> > Чтобы поменять пароль, просто введите команду еще раз с новым паролем. Старый затрется.
> > ```bash
> > Router(config)# enable secret new_strong_pass
> > ```

---

## 🔗 Связи
*  **Теория Аутентификации:** [[Cs - 02 Authentification | Authentification]]
* **Команда перехода:** [[Cs - enable | enable]] (куда мы ставим этот замок)
* **Конкурент (плохой):** `enable password` (никогда не используй)