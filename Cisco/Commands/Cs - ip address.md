---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw (L3)
📶 Mode:
  - ⚙️ Config-If
🗂️ Category:
  - 🔌 Interfaces
  - 🛣️ Routing
📝 Description: Назначение логического IPv4-адреса и маски подсети интерфейсу
⚡ Alias: ip add
💻 Command: ip address
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `ip address` присваивает интерфейсу **логический адрес**.
> Это превращает физический порт ("просто дырку для кабеля") в полноценный сетевой узел, который может отправлять и принимать пакеты.
> 
> **Ключевая логика:**
> 1.  Адрес привязывается к интерфейсу, а не к устройству целиком. (У роутера может быть 10 разных IP-адресов на 10 разных портах).
> 2.  Вместе с адресом **всегда** указывается **Маска подсети**. Она говорит роутеру: *"Какая часть этого адреса — это улица (Сеть), а какая — номер дома (Хост)"*.
> 3.  Как только адрес назначен и порт включен (`no shutdown`), роутер **автоматически** добавляет эту сеть в свою **Таблицу Маршрутизации** (Routing Table) как `Directly Connected`.

---

> [!NOTE]+ 🏗️ 1. Характеристики и Поведение
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `(config-if)#` (Внутри интерфейса) |
> | **Обязательность маски** | **Абсолютная.** Cisco IOS не умеет угадывать маску (в отличие от Windows). Вы обязаны ввести её явно. |
> | **Поведение при повторном вводе** | **ПЕРЕЗАПИСЬ (Overwrite).** Если на порту уже был адрес `10.0.0.1` и вы вводите `ip address 192.168.1.1...`, старый адрес **исчезает**. |
> | **Проверка на конфликты** | Роутер не даст задать адреса из одной и той же подсети на **разные** интерфейсы. (Например, нельзя дать `192.168.1.1` на `Gi0/0` и `192.168.1.2` на `Gi0/1` — роутер выдаст ошибку `Overlaps with...`). |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> ip address <IP-ADDRESS> <SUBNET-MASK> [secondary]
> ```
> * **Удаление адреса:**
> ```bash
> no ip address
> ```
> *(Порт становится чисто канальным L2 или просто выключается логически).*

> [!NOTE]- 🧠 3. Глубокий разбор: Маска подсети
> Роутер понимает маску только в десятичном формате (dotted-decimal).
> 
> | CIDR (Слэш) | Десятичная маска (Cisco) | Количество адресов | Назначение |
> | :--- | :--- | :--- | :--- |
> | `/8` | `255.0.0.0` | 16 млн. | Огромные сети (Class A). |
> | `/16` | `255.255.0.0` | 65 536 | Средние сети (Class B). |
> | `/24` | `255.255.255.0` | 254 | Стандарт для офиса/дома (Class C). |
> | `/30` | `255.255.255.252` | **2** | **Point-to-Point** (соединение двух роутеров). |
> | `/32` | `255.255.255.255` | 1 | **Loopback** (адрес конкретного устройства). |

> [!TIP] 🔄 4. Продвинутые сценарии
> 
> **А. Вторичные адреса (Secondary IP)**
> Иногда нужно, чтобы один порт работал сразу в двух разных сетях (например, при миграции офиса со старой адресации на новую).
> * Если ввести команду просто так — она затрет старый адрес.
> * Если добавить слово `secondary` — адрес добавится **вторым**.
> ```bash
> Router(config-if)# ip address 192.168.1.1 255.255.255.0
> Router(config-if)# ip address 10.0.0.1 255.0.0.0 secondary
> ```
> *Теперь порт отвечает и на 192.168..., и на 10.0...*
> 
> **Б. Получение адреса от провайдера (DHCP Client)**
> Роутер может сам выступать в роли "клиента" (как твой домашний ПК), если он подключен к провайдеру.
> ```bash
> Router(config-if)# ip address dhcp
> ```
> *Роутер пошлет запрос и получит IP автоматически.*

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Стандартная настройка шлюза (Gateway)
> > Самый частый кейс. Мы делаем этот порт "Шлюзом по умолчанию" для компьютеров в сети.
> > ```bash
> > Router(config)# interface gi0/0
> > Router(config-if)# ip address 192.168.10.1 255.255.255.0
> > Router(config-if)# no shutdown
> > ```
> 
> > [!EXAMPLE]- Настройка канала между роутерами (Point-to-Point)
> > Для соединения двух роутеров не нужна большая сеть. Используют маску `/30` (всего 2 адреса), чтобы сэкономить IP.
> > ```bash
> > Router(config)# interface serial 0/0/0
> > Router(config-if)# ip address 10.1.1.1 255.255.255.252
> > Router(config-if)# no shutdown
> > ```

---

## 🔗 Связи
* **Что происходит в "мозгах" роутера:** Появление записи в [[Cs - show ip route]] (Таблица маршрутизации).
* **Проверка:** [[Cs - show ip interface brief]] (Кратко) или `show interface <name>` (Подробно с маской).
* **Конфликты:** Если вы видите ошибку `% IP addresses may not be configured on L2 links`, значит вы пытаетесь задать IP на свитче, который не переведен в режим L3 (нужна команда `no switchport` или использование `interface vlan`).