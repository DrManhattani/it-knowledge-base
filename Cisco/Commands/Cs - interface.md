---
tags:
  - 🌐/cisco
  - 🌐/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - 🔌 Interfaces
  - 🏗️ Architecture
📝 Description: "Фундаментальный разбор интерфейсов: физическая иерархия, логика именования, виртуализация и управление состоянием."
⚡ Alias: int
💻 Command: interface
---
# 📖 Глава: Команда interface и архитектура портов

Команда `interface` — это одна из "несущих стен" в конфигурации Cisco IOS. В отличие от многих других команд, которые просто меняют параметры, команда `interface` **меняет контекст**. Она переносит администратора из глобального управления устройством в режим микроменеджмента конкретного узла.

Чтобы понять эту команду, нужно сначала разобраться, что такое "Интерфейс" в понимании Cisco.

---

## 🟢 Уровень 1. Базовое понимание (Для новичков)

### Что такое интерфейс?
В бытовом понимании интерфейс — это "дырка", куда втыкается кабель. Но для операционной системы IOS интерфейс — это **объект**, у которого есть набор свойств (скорость, адрес, статус).

Когда вы вводите `interface gigabitEthernet 0/0`, вы говорите роутеру:
> *"Я хочу сфокусировать всё свое внимание на объекте Gi0/0. Все команды, которые я введу дальше (IP-адрес, включение, описание), должны примениться ТОЛЬКО к этому объекту".*

### Как меняется консоль?
Это визуально отображается в приглашении командной строки:
1.  Вы были в `Router(config)#` (Глобальный режим — "коридор").
2.  Вы ввели `interface ...`.
3.  Вы попали в `Router(config-if)#` (Режим интерфейса — "комната").
    * *`if` — это сокращение от interface.*

Чтобы выйти обратно в коридор, используется команда `exit`. Чтобы выйти совсем — `end` (или `Ctrl+Z`).

---

## 🟡 Уровень 2. Логика именования (Анатомия названия)

Почему имена такие сложные — `GigabitEthernet0/0/1`? Почему нельзя просто назвать их `Port1`, `Port2`?
Cisco использует **позиционную нотацию**. Имя порта — это его физический адрес на материнской плате.

Формула: **`<ТИП> <СЛОТ> / <СУБСЛОТ> / <ПОРТ>`**

### 1. Тип (Технология)
Первое слово говорит о том, с какой максимальной скоростью и по какому стандарту работает порт.

| Полное имя | Сокращение | Скорость | Примечание |
| :--- | :--- | :--- | :--- |
| `Ethernet` | `eth` | 10 Мбит/с | Древний стандарт (90-е годы). Сейчас встречается редко. |
| `FastEthernet` | `fa` | 100 Мбит/с | Стандарт для офисных свитчей и старых роутеров. |
| `GigabitEthernet` | `gi` | 1000 Мбит/с (1G) | Современный стандарт для большинства устройств. |
| `TenGigabitEthernet` | `te` | 10 Гбит/с (10G) | Используется в ядрах сети и серверах. |
| `Serial` | `se` | Вариативная | Старый тип WAN-портов для глобальных сетей. |

### 2. Цифры (География порта)
Цифры через слэш (`/`) указывают точное местоположение.

* **Формат `X/Y` (Две цифры):** Обычно используется в роутерах с фиксированной конфигурацией (ISR 4000, 2900) или свитчах.
    * **Первая цифра (`0`):** Номер слота. `0` обычно означает "впаяно в материнскую плату". `1`, `2` и т.д. — это слоты расширения, куда можно докупить и вставить модули.
    * **Вторая цифра (`0`, `1`...):** Порядковый номер порта на самой плате (слева направо).
    * *Пример `Gi0/1`:* Встроенный слот (0), второй порт слева (1).

* **Формат `X/Y/Z` (Три цифры):** Используется в больших модульных роутерах или стекируемых свитчах.
    * **Первая цифра:** Номер самого устройства в стеке (стопке свитчей) или номер шасси.
    * **Вторая цифра:** Номер слота/модуля.
    * **Третья цифра:** Номер порта.
    * *Пример `Gi2/0/48`:* Это 2-й свитч в стопке, встроенный модуль, 48-й порт.

---

## 🟠 Уровень 3. Виртуализация (Профессиональный уровень)

Команда `interface` умеет работать не только с физическими железками. Она умеет **создавать** виртуальные сущности. Это критически важно для понимания современной сети.

Если вы напишете `interface gigabitEthernet 0/0`, а такого порта физически нет в роутере — вы получите ошибку.
Но есть типы интерфейсов, которые создаются **динамически** в момент обращения к ним.

### 1. Loopback (Интерфейс-петля)
* **Команда:** `interface loopback <НОМЕР>`
* **Что это:** Это "фейковый", полностью программный интерфейс. Он эмулирует наличие порта, который всегда работает.
* **Зачем это нужно:**
    * Физический кабель может порваться, и порт `Gi0/0` упадет (станет Down). IP-адрес на нём станет недоступен.
    * `Loopback` никогда не падает, пока включен сам роутер.
    * *Применение:* На него вешают "Главный IP-адрес роутера" (Router ID) для управления и протоколов OSPF/BGP. Если вы пингуете роутер, надежнее пинговать его Loopback.

### 2. SVI (Switch Virtual Interface) — VLAN интерфейс
* **Команда:** `interface vlan <НОМЕР>`
* **Где работает:** На свитчах (L2/L3) и иногда роутерах.
* **Суть:** Представьте, что внутри свитча есть невидимый внутренний роутер. Команда `interface vlan 10` создает "виртуальную ногу" этого роутера, подключенную к VLAN 10.
* **Применение:**
    * На L2-свитчах: Для управления (чтобы зайти по SSH).
    * На L3-свитчах: Для маршрутизации между отделами (Inter-VLAN routing).

### 3. Sub-interfaces (Субинтерфейсы)
* **Команда:** `interface gi0/0.10` (через точку!)
* **Суть:** Технология "Роутер на палочке" (Router-on-a-Stick). Мы берем один физический порт `Gi0/0` и программно "нарезаем" его на ломтики.
* `Gi0/0.10` обслуживает трафик с меткой VLAN 10.
* `Gi0/0.20` обслуживает трафик с меткой VLAN 20.
* Для роутера это выглядят как разные независимые интерфейсы, хотя провод один.

---

## 🔴 Уровень 4. Продвинутое управление (Expert Tips)

Здесь собраны команды и концепции, которые отличают профи от новичка. Это инструменты для экономии времени и решения нестандартных задач.

### A. Команда `range` (Массовая настройка)
Настраивать 48 портов на свитче по одному — это мучение. Используйте `interface range`.
* **Синтаксис:** `interface range <ТИП> <СПИСОК>`
* **Правило пробелов:** В этой команде Cisco очень капризна к пробелам.
    * `int range fa0/1-5` — ❌ Ошибка.
    * `int range fa0/1 - 5` — ✅ Правильно (пробелы вокруг дефиса).
* **Сложные списки:** Можно комбинировать.
    * `int range fa0/1 - 4 , gi0/1 - 2` — Выбрать порты 1-4 И гигабитные порты 1-2 одновременно.

### B. Команда `default interface` (Очистка)
Как вернуть порт в состояние "как из коробки"?
Многие новички пытаются вручную отменять команды: `no ip address`, `no description`, `no speed`... Это долго и можно что-то забыть.
* **Решение:** `default interface gi0/0`.
* **Результат:** IOS удаляет **все** настройки с этого порта и сбрасывает его в дефолт (обычно это shutdown). Очень полезно в лабораторных работах.

### C. Переключение режимов L2 / L3 (На дорогих свитчах)
Некоторые свитчи (например, Catalyst 3650, 9300) умеют работать и как свитч, и как роутер одновременно.
Порт на таком устройстве может быть в двух состояниях:
1.  **Switchport (L2):** Обычный режим коммутации. Понимает MAC-адреса, VLAN, STP. Не понимает IP-адреса.
    * *Как включить:* `switchport`
2.  **Routed Port (L3):** Режим маршрутизации. Порт ведет себя как на роутере. Можно задать IP, но перестают работать VLAN.
    * *Как включить:* `no switchport`
    * *Важно:* Если вы пытаетесь ввести `ip address` и не видите такой команды, скорее всего, порт в режиме L2. Введите `no switchport` сначала.

---

## 🧠 Итоговая сводка по команде

| Команда / Аргумент | Что делает? | Контекст |
| :--- | :--- | :--- |
| `interface gi0/0` | Вход в настройки физического порта. | База |
| `interface loopback 0` | Создание вечного виртуального интерфейса. | OSPF, Mgmt |
| `interface vlan 10` | Создание SVI (шлюза для VLAN). | L3 Switching |
| `interface gi0/0.10` | Создание субинтерфейса (802.1Q). | RoaS |
| `interface range ...` | Групповой выбор портов. | Скорость работы |
| `default interface ...` | Сброс настроек порта в ноль. | Чистка конфига |
| `no switchport` | Превращение порта свитча в порт роутера. | Advanced L3 |