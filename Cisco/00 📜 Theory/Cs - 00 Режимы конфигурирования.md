---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/router
created: 2026-01-08
status: 🟢 Актуально
---

# 📟 Cisco Router: Command Reference

> [!SUMMARY] О навигации
> В Cisco IOS критически важно понимать, в каком режиме (Prompt) вы находитесь. Команды из одного режима не сработают в другом.
> * **User Exec** (`>`) — только просмотр.
> * **Privileged Exec** (`#`) — администрирование.
> * **Global Config** (`(config)#`) — настройка устройства.

![[Pasted image 20260108174705.png]]
<div style="text-align: center; font-size: 0.9em; color: gray; margin-top: 5px;"> <b>Рис. 1.</b> Схема иерархии режимов Cisco IOS. </div>

---

## 🛠 1. Базовая навигация и Режимы

> [!INFO] Суть
> Команды для перемещения по иерархии доступа. Без них вы не сможете начать настройку.

| Команда            | Режим (Где вводить) | Описание действия                                                              |
| :----------------- | :------------------ | :----------------------------------------------------------------------------- |
| <kbd>enable</kbd>  | `Router>`           | **Вход в режим Admin.** Переводит из ограниченного режима в привилегированный. |
| <kbd>exit</kbd>    | *Любой*             | **Шаг назад.** Возврат на уровень вверх или выход из системы.                  |
| <kbd>disable</kbd> | `Router#`           | **Понижение прав.** Возврат из привилегированного режима в пользовательский.   |
<br>

## ⚙️ 2. Системные настройки (Global Config)

> [!WARNING] Осторожно
> Изменения в этом разделе влияют на работу всего устройства целиком. Вводятся из `Router#` командой <kbd>configure terminal</kbd>.

| Команда                        | Контекст    | Описание действия                                                                              |
| :----------------------------- | :---------- | :--------------------------------------------------------------------------------------------- |
| <kbd>configure terminal</kbd>  | `Router#`   | **Вход в конфиг.** Главная точка входа для любых настроек. Приглашение сменится на `(config)`. |
| <kbd>hostname NAME</kbd>       | `(config)#` | **Имя устройства.** Задает сетевое имя роутера (вместо стандартного "Router").                 |
| <kbd>no ip domain-lookup</kbd> | `(config)#` | **Откл. DNS.** Предотвращает долгий поиск ошибочно введенных команд как доменных имен.         |
![[Pasted image 20260108174809.png]]
<div style="text-align: center; font-size: 0.9em; color: gray; margin-top: 5px;">
    <b>Рис. 2.</b> Базовые глобальные настройки: имя устройства и DNS.
</div>
<br>

## 🔌 3. Настройка Интерфейсов (Сеть)

> [!TIP] Лайфхак
> Не забывайте команду <kbd>no shutdown</kbd>. По умолчанию все порты на роутерах Cisco выключены в целях безопасности.

| Команда | Контекст | Описание действия |
| :--- | :--- | :--- |
| <kbd>interface PORT</kbd> | `(config)#` | **Выбор порта.** Вход в настройки конкретного интерфейса (напр. `interface fa0/0`). |
| <kbd>ip address IP MASK</kbd>| `(config-if)#`| **IP и Маска.** Присвоение адреса (напр. `192.168.1.1 255.255.255.0`). |
| <kbd>no shutdown</kbd> | `(config-if)#`| **Включение.** Физическое включение порта (поднятие линка). |
| <kbd>description TEXT</kbd> | `(config-if)#`| **Описание.** Текстовая пометка, что подключено к этому порту (для админа). |
![[Pasted image 20260108174819.png]]
<div style="text-align: center; font-size: 0.9em; color: gray; margin-top: 5px;">
    <b>Рис. 3.</b> Алгоритм настройки и включения физического интерфейса.
</div>
<br>

## 🛡️ 4. Безопасность и Пароли

> [!Abstract] Защита доступа
> Базовые команды для ограничения физического и удаленного доступа.

| Команда | Контекст | Описание действия |
| :--- | :--- | :--- |
| <kbd>enable secret PASS</kbd>| `(config)#` | **Пароль на Enable.** Защищает вход в привилегированный режим (хранится в зашифрованном виде). |
| <kbd>service password-encryption</kbd> | `(config)#` | **Шифрование.** Заставляет роутер зашифровать все старые открытые пароли в конфигурации. |
![[Pasted image 20260108174835.png]]
<div style="text-align: center; font-size: 0.9em; color: gray; margin-top: 5px;">
    <b>Рис. 4.</b> Команды настройки безопасности (слева) и верификации (справа).
</div>

---

### 🔎 Полезные команды проверки (Show)
*Эти команды вводятся в режиме `Router#`*

* `show running-config` — Показать текущую (активную) настройку.
* `show ip interface brief` — Краткая сводка по всем портам (IP + статус).
* `show version` — Информация о "железе" и версии IOS.