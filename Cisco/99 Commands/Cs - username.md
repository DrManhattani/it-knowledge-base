---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - 🕹️ Mgmt
📝 Description: Создание локального пользователя и пароля (Secure)
⚡ Alias: user
💻 Command: username
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `username` управляет списком пользователей для входа на устройство.
> * **Главное правило:** Используй `secret` (шифрование), а не `password` (текст).

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `(config)#` (Global Configuration) |
> | **Назначение** | Аутентификация (Login) |
> | **Как удалить** | `no username <имя>` |

> [!NOTE]- 💻 2. Синтаксис (Как вводить правильно)
> 
> **Вариант А: Создаем пользователя с нуля (Руками)**
> *Мы не пишем никаких цифр, или пишем 0. Роутер сам всё зашифрует.*
> ```bash
> username <ИМЯ> secret <ПАРОЛЬ>
> ```
> 
> **Вариант Б: Восстанавливаем из бэкапа (Copy-Paste)**
> *Только здесь мы используем цифры 5 или 7, копируя строку из старого конфига.*
> ```bash
> username <ИМЯ> secret 5 <УЖЕ_ЗАШИФРОВАННЫЙ_ХЭШ>
> ```

> [!NOTE]- ⚙️ 3. Магия цифр (Аргументы)
> Если ты нажмешь `?` после слова `secret`, роутер предложит выбрать тип шифрования.
> 
> | Тип | Что это значит | Когда вводить? |
> | :---: | :--- | :--- |
> | **0** | **Я ввожу пароль текстом** | **ВСЕГДА**, когда создаешь нового пользователя. <br>*(Если цифру не писать вообще — это то же самое, что 0).* |
> | **5** | **Я ввожу MD5 хэш** | **НИКОГДА** не пиши руками, если вводишь пароль "cisco". <br>Используй только если вставляешь строку `$1$mERr...` из резервной копии. |
> | **7** | **Я ввожу шифр Cisco** | **НИКОГДА** не пиши руками. <br>Используется только при копировании старых паролей типа `password 7 ...`. |

> [!fail] 🛠️ 4. Как удалить или исправить пользователя
> В Cisco нельзя "отредактировать" пользователя. Если ты ошибся (например, ввел `secret 5 cisco`), нужно удалить и создать заново.
> 
> **Шаг 1. Удаление**
> Ставим `no` перед командой. Достаточно указать только имя.
> ```bash
> Router(config)# no username admin
> ```
> 
> **Шаг 2. Создание заново (Правильно)**
> ```bash
> Router(config)# username admin secret cisco
> ```
> *(Без цифр! Роутер сам превратит это в `secret 5 $1$...` в конфиге)*.

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Обычный админ
> > ```bash
> > Router(config)# username admin secret myStrongPass
> > ```
> > После нажатия Enter в конфиге это превратится в:
> > `username admin secret 5 $1$s8a...`
>
> > [!EXAMPLE]- Админ с полными правами (root)
> > ```bash
> > Router(config)# username admin privilege 15 secret cisco
> > ```

---

## 🔗 Связи
* **Где вводим:** [[Cs - configure terminal | configure terminal]]
* **Для чего нужно:** [[Cs - login local]]
* **Теория Аутентификации:** [[Cs - 01 Authentification| Authentification]]