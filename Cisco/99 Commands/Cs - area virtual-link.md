---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw (L3)
📶 Mode:
  - ⚙️ Config-Router
🗂️ Category:
  - 🩹 Fix
  - 🌉 Connectivity
📝 Description: Создает логический туннель через транзитную зону для соединения изолированной зоны с магистралью (Area 0). Используется как временное решение или для восстановления целостности Backbone.
⚡ Alias: area 1 virt 2.2.2.2
💻 Command: area <transit-area-id> virtual-link <remote-router-id>
---
# 📖 Глава: OSPF Virtual Link

**Virtual Link** — это "виртуальный кабель", который вы протягиваете сквозь чужую зону, чтобы соединить разорванные части OSPF.

В OSPF есть строгое правило иерархии: **Все зоны обязаны иметь физический контакт с Area 0.**
Если у вас есть цепочка `Area 0` -> `Area 1` -> `Area 2`, то **Area 2 изолирована**. Она не сможет обмениваться маршрутами, так как у неё нет выхода в Backbone.
Virtual Link решает эту проблему, создавая туннель через Area 1.

---

> [!INFO]+ 🏗️ 1. Когда это нужно? (Use Cases)
> 1.  **Daisy-chaining (Цепочка):** Подключение зоны, которая физически не касается Area 0 (как в примере выше).
> 2.  **Лечение Backbone:** Если Area 0 разорвалась на две части (Discontiguous Backbone), Virtual Link может сшить их обратно через другую зону.
> 3.  **Слияние компаний:** Когда нужно срочно объединить две сети, но физику перекладывать долго.

---
## 2. Анатомия настройки

Настройка выполняется на **двух ABR**, между которыми находится "Транзитная зона" (Transit Area).

```bash
area <TRANSIT_AREA_ID> virtual-link <REMOTE_ROUTER_ID>
```
> [!INFO]+ 🧩 Разбор аргументов
> | Аргумент | Описание и Нюансы |
> | :--- | :--- |
> | **TRANSIT_AREA_ID** | Номер зоны, через которую проходит туннель. Это зона-"посредник", которая лежит между вами и целью. |
> | **REMOTE_ROUTER_ID** | **RID** (Router ID) соседа на *другом* конце туннеля.<br>⚠️ **Важно:** Это НЕ IP-адрес интерфейса, это именно RID, который сосед выбрал при старте OSPF. |

> [!EXAMPLE]+ 🚀 Пример конфигурации
> **Дано:**
> - **R1** граничит с Area 0 и Area 1. (RID: `1.1.1.1`)
> - **R2** граничит с Area 1 и Area 2. (RID: `2.2.2.2`)
> - **Задача:** Подключить Area 2 к Backbone.
> - **Решение:** Проложить Virtual Link через **Area 1**.
> 
> **Ситуация:** Соединяем **R1** (Backbone) и **R2** (Isolated) через транзитную **Area 1**.
> 
> **Конфигурация R1 (Сторона Backbone):**
> ```bash
> Router(config)# router ospf 1
> ! Указываем RID роутера R2 (цель)
> Router(config-router)# area 1 virtual-link 2.2.2.2
> ```
> 
> **Конфигурация R2 (Сторона изолированной зоны):**
> ```bash
> Router(config)# router ospf 1
> ! Указываем RID роутера R1 (цель)
> Router(config-router)# area 1 virtual-link 1.1.1.1
> ```

## 3. Жесткие ограничения ⛔

Virtual Link — капризная технология. Она **не заработает**, если:

1.  **Stub/NSSA Area:** Транзитная зона (через которую идет туннель) **не может** быть Stub, Totally Stub или NSSA. Это должна быть обычная (Standard) зона.
2.  **No Full Mesh:** Если транзитная зона нестабильна, Virtual Link (который технически является интерфейсом Point-to-Point) будет падать.
3.  **Authentication:** Если в транзитной зоне включена аутентификация, её нужно отдельно настраивать и для Virtual Link.

---
## 4. 🕵️ Нюансы уровня Senior (Deep Dive)

На собеседованиях часто спрашивают "механику под капотом".

> [!QUESTION] Как роутер узнает IP-адрес соседа, если мы задали только RID?
> Мы пишем `virtual-link 2.2.2.2`. Но `2.2.2.2` — это просто имя (RID), оно может быть на Loopback'е, который даже не участвует в OSPF. Куда отправлять пакеты?
> 
> **Ответ: Рекурсивный поиск через Транзитную Зону.**
> 1.  R1 смотрит в **LSDB Area 1** (транзитной зоны).
> 2.  Он ищет там **Router LSA (Type 1)**, созданную роутером `2.2.2.2`.
> 3.  В этой LSA он находит IP-адрес интерфейса, через который `2.2.2.2` подключен к Area 1.
> 4.  Именно этот физический IP используется как *Destination IP* для Unicast-пакетов туннеля.

> [!QUESTION] Чему равен Cost (метрика) виртуального линка?
> Вы не можете задать `ip ospf cost` на виртуальном интерфейсе.
> 
> **Ответ: Стоимость динамическая.**
> Это сумма стоимостей всех физических линков внутри транзитной зоны, по которым проходит путь до удаленного ABR. Если внутри транзитной зоны изменится маршрут — автоматически изменится и стоимость Virtual Link.

> [!NOTE] Тип интерфейса
> Для OSPF виртуальный канал — это интерфейс типа **Unnumbered Point-to-Point**. Он не имеет своего IP-адреса, а "одалживает" IP у физического интерфейса транзитной зоны.

---

> [!WARNING]+  Это "Костыль" (Band-Aid)
> Cisco рекомендует использовать Virtual Link только как **временное решение**.
> 
> * Он усложняет траблшутинг.
> * Он скрывает проблемы плохого дизайна сети.
> * LSA, проходящие через Virtual Link, имеют метку `DNA` (Do Not Age), что нагружает базу данных.
> 
> **Хороший дизайн:** Протянуть кабель или GRE-туннель, чтобы Area 2 касалась Area 0 физически.
---

## 🔗 Связи

- **Диагностика:** Проверка состояния линка [[Cs - show ip ospf virtual-links | show ip ospf virtual-links]].
- **Транзитная зона:** Типы зон, которые блокируют VL [[Cs - ospf area types | ospf area types]].