---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
🗂️ Category:
  - 🔌 Lines
📝 Description: Полный разбор линий управления (Console, VTY, AUX)
⚡ Alias: line con 0
💻 Command: line console 0
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `line console 0` открывает настройки **физического консольного порта**.
> Однако, чтобы понять её смысл, нужно разобраться в концепции "Линий" (Lines) в Cisco IOS в целом.
> 
> **Line (Линия)** — это любой канал (интерфейс), через который человек может отправлять текстовые команды операционной системе роутера.

---

> [!INFO]+ 🧩 1. Теория: Анатомия Линий (Виды входов)
> Представьте, что Роутер — это **Секретный Бункер**. В него можно попасть тремя способами. У каждого способа есть своя "дверь" (Line).
> 
> ### 1. CONSOLE (Физическая дверь)
> * **Что это:** Специальный разъем на корпусе (обычно подписан синим `CONSOLE`).
> * **Тип доступа:** **Out-of-Band** (Внеполосный). Это значит, что управление идет *мимо* основной сети. Даже если роутер стер себе все IP-адреса, сжег сетевые карты и отключился от интернета — этот порт работает.
> * **Как подключаемся:** Ногами идем к роутеру, втыкаем специальный **голубой кабель** в порт Console, другой конец — в USB ноутбука.
> * **Когда используем:** 
> 	1.  *Первичная настройка:* Когда роутер только достали из коробки (у него еще нет IP, по сети не зайти).
>     2.  *Авария:* Когда сеть сломалась и SSH не работает.
>     3.  *Сброс пароля:* Единственный способ взломать свой же роутер, если забыл пароль.
> * **Нумерация:** Обычно `line console 0` (так как порт всего один).
> 
> ### 2. VTY (Виртуальный телепорт)
> * **Что это:** Это не физическая дырка. Это **программный шлюз** внутри операционной системы. 
> * **Тип доступа:** **In-Band** (Внутриполосный). Вы подключаетесь через обычные сетевые кабели (LAN/WAN) вместе с остальным трафиком (YouTube, почта).
> * **Как подключаемся:** Через программы PuTTY/Terminal по протоколам **SSH** или **Telnet**. Для этого на роутере должен быть настроен IP-адрес.
> * **Когда используем:** В 99.9% случаев. Инженер сидит в теплом офисе (или дома) и управляет роутерами по всей стране.
> * **Нумерация:** `line vty 0 4` (или `0 15`). Это значит, что роутер создает 5 или 16 виртуальных потоков, чтобы несколько админов могли зайти одновременно.
> 
> ### 3. AUX (Аварийный лаз)
> * **Что это:** Старый физический порт (Auxiliary).
> * **Суть:** Раньше туда втыкали **телефонный модем**. Если сеть (Internet) падала, админ мог позвонить на этот модем по городской телефонной линии и попасть в консоль.
> * **Сейчас:** Почти не используется. В современных лабах про него можно забыть.

---

> [!NOTE]+ 🏗️ 2. Характеристики (Line Console 0)
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | `(config)#` |
> | **Куда попадаем** | `Router(config-line)#` |
> | **Приоритет** | Самый высокий. Сообщения загрузки (Boot) видны только здесь. |
> | **Зависимость от сети** | Никакой. Работает без IP-адресов. |

> [!NOTE]- 💻 3. Синтаксис
> ```bash
> line console 0
> ```

> [!NOTE]- ⚙️ 4. Ключевые команды настройки (Must Have)
> Эти команды вводятся внутри `(config-line)#`. Они делают работу комфортной.
> 
> | Команда | Описание | Зачем это нужно? |
> | :--- | :--- | :--- |
> | `logging synchronous` | **Синхронизация вывода** | **Критически важно.** Без этой команды, если роутер решит что-то сообщить (например, "Кабель отключен!"), он разорвет ту команду, которую вы сейчас пишете. <br>С этой функцией он "перерисует" вашу строку заново под сообщением. <br>Иначе говоря, будет блокироваться вывод служебных сообщений в тот момент, пока мы что-то вводим|
> | `exec-timeout <мин> <сек>`| **Авто-выход** | По умолчанию роутер выкидывает из консоли через 10 минут бездействия (для безопасности). <br>В Лабораторных работах это бесит, поэтому ставят `exec-timeout 0 0` (отключить таймер). <br>В Реальности ставят `exec-timeout 5 0` (5 минут). |
> | `login local`| **Проверка "Паспорта"** | Говорит роутеру: *"Не пускай никого, пока не проверишь логин и пароль по базе `username`"*. Самый надежный метод. |
> | `password <pass>` + `login` | Простой пароль | (Устарело) Один пароль на всех, без логина. Менее безопасно. |

> [!NOTE]- 🚀 5. Сценарий из жизни (Workflow)
> 
> 1.  **День 1 (Инсталляция):**
>     Инженер приходит в серверную с ноутбуком и **голубым кабелем**.
>     Подключается в Console.
>     ```bash
>     Router(config)# line console 0
>     Router(config-line)# logging synchronous   <-- Чтобы удобно писать
>     Router(config-line)# login local           <-- Чтобы закрыть дверь на замок
>     Router(config-line)# exit
>     Router(config)# interface gi0/0
>     Router(config-if)# ip address 192.168.1.1 ... <-- Настраивает сеть
>     ```
> 2.  **День 2 (Работа):**
>     Инженер сидит дома. Голубой кабель убран в шкаф.
>     Он заходит через **VTY (SSH)** на адрес 192.168.1.1.
>     Настройки, которые он сделал в `line console 0`, на него сейчас **не действуют** (потому что он зашел через другую дверь — VTY).

---

## 🔗 Связи
* **Кого пускаем в эту дверь:** [[Cs - username | username]]
* **Соседняя (виртуальная) дверь:** [[Cs - line vty | line vty]]
* **Команда удобства:** [[Cs - logging synchronous | logging synchronous]]
* **Теория Аутентификации:** [[Cs - 01 Authentification| Authentification]]