---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode: 🔄 Multi
🗂️ Category: 🕹️ Mgmt
📝 Description: Выход на уровень выше или разрыв сессии
⚡ Alias: ex
💻 Command: exit
---

# 💡 Описание
> [!SUMMARY] Суть
> Команда `exit` используется для навигации "назад".
> * Она возвращает пользователя **на один уровень вверх** по иерархии режимов.
> * Если ввести её в самом верхнем уровне (User Mode), она **разорвет сессию** (отключит вас от устройства).

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | Любой режим |
> | **Действие** | Шаг назад (Back) |
> | **Аналог Linux** | `exit` или `Ctrl+D` |
> | **Альтернатива** | `end` (сразу домой) |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> exit
> ```

> [!NOTE]- ⚙️ 3. Аргументы
> *Нет аргументов.*

> [!NOTE]- 📝 4. Руководство (Usage Guidelines)
> 1.  🪜 **Принцип "Лестницы":**
>     * Из `(config-if)#` (Интерфейс) → кидает в `(config)#` (Глобальный).
>     * Из `(config)#` (Глобальный) → кидает в `Priv #` (Привилегированный).
>     * Из `Priv #` → кидает в `User >`.
>     * Из `User >` → **Disconnect** (Закрытие окна PuTTY/консоли).
> 2.  🆚 **Exit vs End:**
>     * `exit` — аккуратно выходит по одной ступеньке.
>     * `end` (или `Ctrl+Z`) — катапультирует из любой глубины настроек сразу в привилегированный режим (`#`).
> 3.  💾 **Нюанс сохранения:** При выходе из режима конфигурации (`exit` или `end`), Cisco помечает текущий конфиг как "измененный", но **не сохраняет** его на диск автоматически.

> [!NOTE]- 🚀 5. Примеры использования
>
> > [!EXAMPLE]+ Пошаговый выход (Step-by-Step)
> > ```bash
> > Router(config-if)# exit
> > Router(config)# exit
> > Router# exit
> > Connection closed by foreign host.
> > ```
>
> > [!EXAMPLE]- Разрыв сессии (Logout)
> > Быстрое завершение работы.
> > ```bash
> > Router# exit
> > ```

---

## 🔗 Связи
* **Альтернатива:** [[end]] (Быстрый выход в корень)
* **Вход:** [[Cs - enable|enable]] / [[Cs - configure terminal|configure terminal]]