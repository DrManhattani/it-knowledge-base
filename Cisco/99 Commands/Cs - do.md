---
tags:
  - 🌐network/cisco
  - 🌐network/cisco/cmd
💠Stack: 🟧 Cisco
📟 Device:
  - ◈ Rtr
  - ⌘ Sw
📶 Mode:
  - ⚙️ Config
  - ⚙️ Sub-Config
🗂️ Category:
  - ⚙️ Sys
📝 Description: Выполнение команд привилегированного режима из конфига
⚡ Alias: do
💻 Command: do
---
# 💡 Описание
> [!SUMMARY] Суть
> Команда `do` — это приставка, которая позволяет выполнять команды **просмотра и диагностики** (уровня Privileged Exec), не выходя из режима конфигурации.
> * **Аналогия:** Это как "крикнуть через стену" в соседнюю комнату, не выходя из своей.
> * Позволяет делать `ping`, `show`, `write` прямо во время настройки.

---

> [!NOTE]+ 🏗️ 1. Характеристики
> | Параметр | Значение |
> | :--- | :--- |
> | **Где вводить** | Любой режим конфигурации `(config...)#` |
> | **Что выполняет** | Любую команду режима `Router#` |
> | **Главный минус** | **Не работает автодополнение (TAB) и справка (?)** |

> [!NOTE]- 💻 2. Синтаксис
> ```bash
> do <ЛЮБАЯ_КОМАНДА_ПРИВИЛЕГИРОВАННОГО_РЕЖИМА>
> ```

> [!fail] ⚠️ 3. Важный нюанс (Слепой ввод)
> Когда вы используете `do`, роутер **перестает подсказывать**.
> * Вы пишете: `do show run` (TAB не сработает, `?` не сработает).
> * Вы должны знать команду наизусть и писать её без ошибок.
> * Если ошибетесь в букве — курсор просто вернет ошибку, не показав, где именно проблема.

> [!NOTE]- 🚀 4. Примеры использования
>
> > [!EXAMPLE]+ Просмотр настроек "не отходя от кассы"
> > Вы настраиваете интерфейс и хотите проверить, применился ли IP.
> > ```bash
> > Router(config-if)# do show ip interface brief
> > ```
>
> > [!EXAMPLE]- Сохранение настроек из конфига
> > Обычно `write` (сохранить) работает только в начале. Но с `do` можно сохраняться сразу после настройки.
> > ```bash
> > Router(config)# do write
> > ```
>
> > [!EXAMPLE]- Пинг во время настройки
> > ```bash
> > Router(config)# do ping 8.8.8.8
> > ```

---

## 🔗 Связи
* **Что заменяет:** Постоянные переходы `exit` -> `show` -> `conf t`
* **Частые спутники:** [[show running-config]], [[write]], [[Cs - ping|ping]]