---
tags:
  - 🌐/cisco
💠Stack: 🟧 Cisco
🗂️ Category:
  - 🛡️ Sec
📝 Description: Полная схема паролей и доступа (Console, VTY, Enable)
---
# 🔐 Теория: Иерархия доступа и паролей

В Cisco IOS безопасность строится эшелонами (слоями). Чтобы получить полный доступ, нужно пройти две "двери".

---

## 🚪 Уровень 1: Вход в систему (Access)
Это момент, когда вы только подключаетесь к устройству. Вы попадаете в пользовательский режим `Router>`.

Здесь есть **два подхода** к аутентификации (проверке прав). Они настраиваются внутри линий (`line console 0` или `line vty 0 4`).

### Способ А: "Общий ключ" (Legacy / Слабый)
Используется одна команда `password` прямо в настройках линии.
* **Как это выглядит:** При входе спрашивают **только пароль**. Логин не нужен.
* **Где хранится пароль:** Прямо под строкой `line ...`.
* **Безопасность:** Низкая. Пароль либо виден, либо слабо зашифрован (Type 7).
* **Команды:**
  ```bash
  line console 0
   password cisco
   login
   ```
### Способ Б: "База пользователей" (Recommended / Надежный)

Мы говорим линии: "Иди смотри в общую базу пользователей".

- **Как это выглядит:** При входе спрашивают **Username** и **Password**.
- **Где хранится пароль:** В глобальной базе `username`.
- **Безопасность:** Высокая. Мы можем использовать `secret` (MD5) при создании пользователя.
- **Команды:**
    1. Создаем юзера: `username admin secret cisco`
    2. Привязываем к линии:
   ```bash
    line console 0
	 login local
    ```
## ⚡ Уровень 2: Повышение привилегий (Privilege)

Вы уже вошли (режим `>`), но ничего сломать не можете. Чтобы стать Администратором (режим `#`), нужно пройти вторую проверку.

### Команда `enable`

Когда вы пишете `enable`, роутер спрашивает **Секретный код**.

- **Важно:** Здесь **НЕ спрашивается имя пользователя**. Это единый пароль для всех, кто хочет стать админом.
- **Настройка:** Задается глобально одной командой.

| **Команда**           | **Безопасность**               | **Вердикт**       |
| --------------------- | ------------------------------ | ----------------- |
| `enable password 123` | **Низкая** (Текст или Type 7)  | ❌ Не использовать |
| `enable secret 123`   | **Высокая** (MD5 Hash, Type 5) | ✅ Стандарт        |
## 🌍 Специфика линий (Console vs VTY)

Хотя логика паролей одинаковая, есть нюансы использования.

### 1. Line Console (Физический доступ)
- Это ваша "последняя надежда".
- **Рекомендация:** Всегда настраивайте здесь `login local` (создайте пользователя). Если вы забудете пароль `enable`, но будете знать пароль пользователя, в некоторых системах это может помочь при восстановлении.
### 2. Line VTY (Удаленный доступ: SSH/Telnet)
- Это доступ по сети.
- **Telnet:** Может работать по "Способу А" (просто пароль).
- **SSH (Secure Shell):** Протокол SSH **технически требует** имя пользователя.
    - Поэтому для SSH вариант `password ... login` **НЕ РАБОТАЕТ**.
    - Для SSH **обязательно** нужен `login local` и созданный `username`.

## 🚀 Итоговая схема настройки (Best Practice)
Это "Золотой стандарт" настройки любого устройства Cisco:
1. **Создаем "Паспорт" (User):** `username admin secret MyStrongPass`
2. **Защищаем "Сейф" (Enable):** `enable secret MySecretCode`
3. **Вешаем замок на "Физическую дверь" (Console):** `line console 0` -> `login local`
4. **Вешаем замок на "Сетевую дверь" (VTY/SSH):** `line vty 0 4` -> `login local`