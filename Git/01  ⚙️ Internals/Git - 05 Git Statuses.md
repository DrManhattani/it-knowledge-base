---
tags:
  - 🐙git/theory
  - 🐙git
💠Stack: 🐙 Git
🌍 Scope:
  - 🌎 Global
🛡️ Risk: ✅ Safe
🏹 Target Area: Working Directory & Index 🗂️
📝 Description: Четыре состояния файлов в Git и логика их переходов.
---
# 🔄 Жизненный цикл файлов в Git

> [!ABSTRACT] Суть
> Git не просто следит за файлами, он управляет их **состояниями**. Весь процесс разработки — это перевод файлов из «неотслеженных» в «закоммиченные» через промежуточную зону (Index/Staging).

---
## 🚦 1. Четыре статуса (The Big Four)

| Статус | Имя | Что это значит? | Как попасть? |
| :--- | :--- | :--- | :--- |
| **Untracked** | Неотслеживаемый | Новый файл, о котором Git еще не знает. | Создать новый файл. |
| **Unmodified** | Чистый | Файл в репозитории, совпадает с последним коммитом. | Сделать `commit` или `checkout`. |
| **Modified** | Измененный | Файл изменен, но правки еще не «обещаны» коммиту. | Отредактировать файл. |
| **Staged** | Подготовленный | Файл помечен для включения в следующий снимок. | Выполнить `git add`. |

---
## 🔄 2. Диаграмма переходов

Чтобы лучше понимать механику, представь этот процесс как конвейер:
1.  **Untracked → Staged**: `git add <file>` (Git начинает следить за файлом).
2.  **Staged → Unmodified**: `git commit` (Файл зафиксирован в истории).
3.  **Unmodified → Modified**: Ты просто меняешь текст в редакторе.
4.  **Modified → Staged**: `git add <file>` (Подтверждаешь, что эти изменения пойдут в коммит).
5.  **Modified → Unmodified**: `git checkout -- <file>` (Откат изменений к состоянию коммита).

---
## ⚡ 3. Важные нюансы (Pro Tips)

> [!WARNING] **Ловушка: Файл в двух состояниях одновременно**
> Это часто путает новичков. Файл может быть одновременно **Staged** и **Modified**. 
> **Как это бывает:**
> 1. Ты изменил файл и сделал `git add`. (Файл в индексе, статус **Staged**).
> 2. Ты тут же внес в него еще одну правку, но `add` еще не делал.
> **Итог:** В коммит уйдет только первая версия правки, а вторая останется висеть как **Modified**. Всегда проверяй `git status` перед коммитом!

> [!NOTE] **Пятый статус: Ignored**
> Есть файлы, которые мы вообще не хотим видеть в этом списке (логи, кэш, секреты). 
> - Файлы, прописанные в `.gitignore`, технически остаются `untracked`, но Git перестает их «предлагать» добавить в репозиторий.

---
## 🛠️ 4. Команды для проверки

* `git status` — основной инструмент для проверки состояний.
* `git status -s` (short) — краткий вывод. 
    * `??` — Untracked.
    * `A ` — Added to stage.
    * ` M` — Modified (в рабочей области).
    * `M ` — Modified (в индексе/staged).

---
## 🔗 Связи

> [!LINK] **Взаимодействие с другими узлами базы**
> 
> - **Команды перехода:**
>     
>     - [[Git - git add]]: Переводит файлы из **Untracked** или **Modified** в статус **Staged**.
>         
>     - [[Git - git commit]]: Переводит файлы из **Staged** в статус **Unmodified** (создает объект коммита).
>         
>     - [[Git - git rm]]: С флагом `--cached` переводит файлы из **Staged** обратно в **Untracked**.
>         
> - **Области (Areas):** [[Git - 02 Areas | Areas]]
>         
> - **Инспекция:**
>     
>     - [[Git - git ls-files | git ls-files]]: Позволяет увидеть список файлов, которые находятся в статусах **Staged** или **Unmodified**.
>         
>     - [[Git - git status]]: Главный инструмент для визуализации всех четырех статусов в реальном времени.
>         
> - **Фильтрация:**
>     
>     - [[Git - git ignore]]: Механика, которая позволяет скрывать файлы в статусе **Untracked**, чтобы они не «мозолили глаза» в выводе `status`.