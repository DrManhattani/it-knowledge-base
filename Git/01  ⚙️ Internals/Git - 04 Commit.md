---
tags:
  - 🐙git/theory
  - 🐙git
  - 🐙git/commit
💠Stack: 🐙 Git
🌍 Scope:
  - 🌎 Global
🛡️ Risk: ✅ Safe
🏹 Target Area: Object DB 🗄️
📝 Description: "Коммит как фундаментальный узел истории: структура, метаданные и связь объектов."
💻 Command: git commit
---
# 📦 Объект Commit: Снимок состояния

> [!ABSTRACT] Суть
> **Commit** — это объект, который фиксирует определённое состояние репозитория. Он содержит ссылку на объект дерева (tree), который отражает текущее состояние файлов, а также метаданные (автора, сообщение коммита, дату и ссылку на предыдущий коммит). Если файл не менялся с прошлого раза, коммит просто ссылается на уже существующий объект (blob) в базе.

---

> [!example] 🔍1. Разбор вывода терминала
> ```
> [master (root-commit) 9c45069] Our first commit
> 3 files changed, 154 insertions(+)
> create mode 100644 1. Введение/1. Git & Github
> create mode 100644 1. Введение/2. Z shell
> create mode 100644 1. Введение/2.1 Установка ZSH
> ```

> [!INFO] **Anatomy: [master (root-commit) 9c45069]**
> 
> | Элемент | Значение | Описание |
> | :--- | :--- | :--- |
> | `master` | **Ветка** | Текущая ветка, куда записан коммит (указатель на хеш). |
> | `root-commit` | **Статус** | Самый первый коммит в истории (нет родителя). |
> | `9c45069` | **Хеш** | Короткий ID коммита (первые 7 символов SHA-1). |
> | `Our first...` | **Message** | Заголовок (первая строка) твоего сообщения. |

> [!ABSTRACT] **Статистика изменений (Delta Stats)**
> 
> `3 files changed, 154 insertions(+)`
> 
> - **Файлы**: Количество измененных, добавленных или удаленных файлов.
>     
> - **Вставки** (`+`) Суммарное количество добавленных строк.
>     
> - **Удаления** (`-`): Количество удаленных строк (в твоем примере их 0).
>     
> 
> _Git сравнивает текущий снимок (Snapshot) с предыдущим, чтобы вывести эту разницу для человека._

> [!SETTINGS] **Режимы файлов (File Modes)**
> 
> `create mode 100644 1. Введение/1. Git & Github`
> 
> - `create mode` Файл абсолютно новый для репозитория.
>     
> - `100`: Тип объекта — **обычный файл** (regular file).
>     
> - `644`: Права доступа (Unix permissions):
>     
>     - **6** (владелец): Чтение + Запись ($4 + 2$).
>         
>     - **4** (группа): Только чтение.
>         
>     - **4** (остальные): Только чтение.
>         
> 
> _Примечание: Если файл исполняемый (скрипт), режим будет `100755`._

---
## 🏗️ 2. Внутренняя структура (Internal Structure)

Если заглянуть внутрь объекта коммита (через `git cat-file -p`), мы увидим строгий набор полей:

| Поле | Описание |
| :--- | :--- |
| **tree** | Ссылка на объект дерева (структуру папок и файлов). |
| **parent** | Хеш предыдущего коммита (у `root-commit` отсутствует). |
| **author** | Кто написал код + метка времени. |
| **committer** | Кто применил коммит в репозиторий + метка времени. |
| **message** | Твое описание изменений. |
> [!TIP] Формула коммита
> Хеш коммита вычисляется от всех этих данных. Если ты изменишь хотя бы одну букву в сообщении или секунду в дате, хеш станет совершенно другим:
> $$Commit\_SHA1 = \text{Hash}(\text{tree} + \text{parent} + \text{author} + \text{committer} + \text{message})$$

---
## ⚡ 3. Важные нюансы (Nuances)

### 👤 Автор vs Коммиттер
В Git это два разных человека.
* **Author**: Тот, кто физически набрал код.
* **Committer**: Тот, кто отправил этот код в историю (например, принял твой Pull Request или сделал `rebase`).
Обычно они совпадают, но в командной работе это помогает отследить реальную историю изменений.
### 🧩 Неизменяемость (Immutability)
Коммит нельзя «отредактировать». Когда ты делаешь `git commit --amend`, Git не меняет старый объект. Он создает **новый** объект коммита с новым хешем, а старый со временем удаляется сборщиком мусора (`gc`).

---
## 🌟 4. Best Practices: Как делать это правильно

> [!TIP] **Атомарные коммиты (Atomic Commits)**
> 
> **Правило:** Один коммит = одна задача (одна логическая правка).
> 
> - **Плохо:** «Починил баг, обновил стили и добавил заметку про VLAN». (Если баг вернется, придется откатывать и стили, и заметки).
>     
> - **Хорошо:** Три разных коммита. Это позволяет легко делать `revert` (отмену) только одной части.
>     

> [!IMPORTANT] **Conventional Commits (Стандарт сообщений)**
> 
> В крупных проектах сообщения пишут по шаблону: `type(scope): description`.
> 
> - `feat:` — новая функциональность.
>     
> - `fix:` — исправление ошибки.
>     
> - `docs:` — работа с документацией (как у тебя сейчас).
>     
> - `style:` — правка пробелов, форматирования (не влияет на логику).
>     
> 
> _Пример:_ `docs(git): добавить разбор вывода git commit`

---
## 🔗 5. Взаимосвязанные действия (The Ecosystem)

Коммит не живет в вакууме. Вот команды, которые чаще всего «идут в комплекте»:

> [!EXAMPLE] **Операции с коммитами на практике**
> 
> | Команда | Зачем нужна на работе |
> | :--- | :--- |
> | `git commit --amend` | **«Забыл добавить файл»**. Позволяет доложить изменения в последний коммит, не создавая новый «мусорный» узел. |
> | `git revert <hash>` | **«Всё сломалось, верни как было»**. Создает новый «анти-коммит», который отменяет изменения старого. Безопасно для общих веток. |
> | `git cherry-pick <hash>` | **«Хочу этот фикс себе»**. Позволяет скопировать конкретный коммит из одной ветки в другую. |
> | `git log --oneline --graph` | **«Где я?»**. Визуализация дерева коммитов. Помогает понять, как ветки расходятся и сливаются. |

---
## 🛠️ 6. Практический нюанс: Пустые коммиты

Иногда на работе нужно запустить «автоматизацию» (CI/CD) или просто пометить важный этап без изменения кода.
- **Команда:** `git commit --allow-empty -m "trigger: запуск сборки"`
- Это создаст полноценный объект коммита, который ссылается на то же дерево (`tree`), что и предыдущий.

---
## 🧠 Deep Dive: Связь с HEAD

> [!QUESTION] Кто такой HEAD?
> 
> В твоем конспекте важно упомянуть, что `HEAD` — это «указатель на текущее место».
> 
> - Обычно `HEAD` указывает на **ветку** (`main`), а ветка указывает на **последний коммит**.
>     
> - Когда ты делаешь новый коммит, Git просто передвигает `main` на новый хеш, а `HEAD` послушно следует за ним.
>     

---
## 🔗 Связи
- **Команда для создания коммита:** [[Git - git commit | git commit ]]
- **Области в Git :** [[Git - 02 Areas | Areas]]
- **Типы объектов в Git:** [[Git - 00 Objects Type | Objects Type]]
