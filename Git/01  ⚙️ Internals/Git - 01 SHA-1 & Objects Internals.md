---
tags:
  - 🐙git/integrity
  - 🐙git/objects
  - 🐙git/crypto
💠Stack: 🐙 Git
🌍 Scope:
  - 🌎 Global
🛡️ Risk: ✅ Safe
📝 Description: Механика хеширования в Git. Алгоритм SHA-1, структура заголовков объектов и права доступа (File Modes).
💻 Command:
  - git hash-object
  - shasum
---
# 🐙 Глава: Hash-функции и SHA-1
*Integrity & Algorithms*

> [!ABSTRACT]+ 🧮 Hash-функция
> Это односторонняя криптографическая функция, математический алгоритм, который преобразует входные данные **произвольной** длины в строку **фиксированной** длины. Результат выполнения называется **хэш-значением** или **хэшем**.
> * **Свойства:** 
> 	* **Детерминированность:** Один и тот же вход всегда дает один и тот же выход.
> 	* **Необратимость:** Из фарша (хэша) нельзя восстановить корову (исходный файл).
> 	* **Лавинный эффект:** Изменение 1 бита данных меняет хэш до неузнаваемости.

| **Алгоритм** | **Бит** | **Символов** | **Статус**     | **Где применяется / Особенности**                                                                                                                      |
| ------------ | ------- | ------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **MD5**      | 128     | 32           | ⛔ **Broken**   | **Устарел.** Найдены коллизии. Сейчас используется только как checksum для проверки целостности файлов (не для защиты!), так как он очень быстрый.     |
| **SHA-1**    | 160     | 40           | ⚠️ **Weak**    | **Стандарт Git.** Теоретически взломан (атака SHAttered). Git пока использует его для совместимости, но медленно мигрирует на SHA-256.                 |
| **SHA-256**  | 256     | 64           | ✅ **Standard** | **Золотой стандарт.** Используется в SSL/TLS сертификатах, Bitcoin, Docker образах. Будущее Git. Оптимальный баланс скорости и защиты.                 |
| **SHA-384**  | 384     | 96           | 🔒 **High**    | Усеченная версия SHA-512. Используется в правительственных стандартах (NSA Suite B), где нужна защита от специфических атак (length extension attack). |
| **SHA-512**  | 512     | 128          | 🛡️ **Max**    | Максимальная длина. **Нюанс:** На 64-битных процессорах часто работает **быстрее**, чем SHA-256, благодаря оптимизации вычислений.                     |
>[!NOTE] Почему так много версий?
>С ростом мощности компьютеров старые хэши становятся уязвимы к **брутфорсу** и **коллизиям** (когда для двух разных файлов получается одинаковый хэш).
>- **MD5** сломали в 2000-х.
>- **SHA-1** "пошатнули" в 2017 (Google).
>- **SHA-256** на данный момент считается абсолютно надежным.
---

## 1. 🔑 SHA-1
*Secure Hash Algorithm 1*

Это криптографический алгоритм, который преобразует произвольные данные в строку фиксированной длины **160 бит**.

* **В контексте Git:** Используется для создания уникальных идентификаторов всех объектов.
* **Формат:** Хэш состоит из **40 символов** в шестнадцатеричном представлении (0-9, a-f).
    * *Математика:* 1 символ = 4 бита. 40 символов * 4 = 160 бит.
* **Детерминированность:** Один и тот же набор данных всегда приводит к одному и тому же хэшу.
    * *Пример:* Если два файла идентичны по содержимому, они будут иметь один и тот же SHA-1 хэш.

```mermaid
graph LR
    Input[📄 Файл: 'report.txt'] -->|Algorithm| SHA1(⚙️ SHA-1)
    SHA1 -->|Result| Hash[🏷️ a1b2c3d4...]
    
    style SHA1 fill:#f9f,stroke:#333,stroke-width:2px
    style Hash fill:#bfb,stroke:#333,stroke-width:2px
```
---
## 2. 🛠️ Команда shasum

Команда предназначена для проверки и вычисления хэшей данных. Предоставляет интерфейс к различным версиям SHA.

```bash
shasum <файл>                          # Обычный вид (SHA-1 по дефолту)
shasum -a <1|224|256|384|512> <файл>   # Флаг -a выбирает версию алгоритма
```

## 3. 🏗️ Как создается SHA-1 хэш файла

> [!WARNING] Многие думают, что Git хеширует *просто* содержимое файла - **это не так!**
> * Если вы сделаете `sha1sum myfile.txt`, хэш *не совпадет* с тем, что дает Git. 
> * **Почему?** Git добавляет **Заголовок (Header)** перед хешированием.

> [!EXAMPLE] 📐 Формула расчета
> $$Hash = SHA1({Header} + \backslash0 + {Content})$$
> **Расшифровка:**
> * **Header:** `<Тип объекта> <Пробел> <Размер в байтах>`
> * **Delimiter:** `\0` (Null-byte, байт со значением 0). 
> 	* **Разделитель** - последовательность из символов, указывающая на границу между отдельными регионами в тексте.
> * **Content:** Реальное содержимое файла.

---
### 📄 3.1 Для типа **BLOB** (Файлы)
> [!EXAMPLE] Хранит **содержимое** файла.
> 📐 Формула для BLOB:
> $$Hash = SHA1({Type} + {Size} + \backslash0 + {Data})$$
**Пример содержимого файла blob:**
>```
blob 11\0Hello, Git!
>```

> [!TIP] 👨‍🔬 Лабораторная проверка (Proof of Concept)
> Мы можем доказать это, сравнив ручной расчет и команду Git.
> 
> **1. Ручной расчет (через shasum)**
> ```bash
> echo -ne 'blob 11\0Hello, Git!' | shasum
> ```
> 
> **2. Расчет через Git**
> ```bash
> echo 'Hello, Git!' | git hash-object --stdin
> ```
> *Они выдадут одинаковый хэша.*

> [!WARNING] Важно про `echo` и `\0`
> ⚠️ **Нюанс** с `echo`: Команда по умолчанию добавляет невидимый перенос строки (`\n`) в конце. **Это меняет хэш!**
> &nbsp;&nbsp;&nbsp;&nbsp; *Решение:* Используйте `echo -n` (без переноса) или `printf`.
> 
> ⚠️ **Нюанс** с `\0`: Это **Null-byte** (разделитель). Не все оболочки интерпретируют его одинаково. Bash может понять его как просто два символа `\` и `0`.
> &nbsp;&nbsp;&nbsp;&nbsp; *Решение:* Используйте флаг `-e` в команде `echo` (enable interpretation). Без него `echo` выведет строку "как есть".
### 🌳 3.2 Для типа **TREE** (Папки)
> [!EXAMPLE] Дерево хранит структуру и имена.
> 📐 Формула для BLOB:
> $$Hash = SHA1({Rights} + {Type} + {Hash Object} + {Tab} + {Name})$$
**Пример содержимого файла tree:** 
>```
100644 blob b7aec520dec0a7516c18eb4c68b64ae1eb9b5a5e     file_name
>```
### 💾 3.3 Для типа **COMMIT** (Контрольная точка)
> [!example]  Коммит связывает дерево с историей и автором.
**Пример содержимого файла commit:**
>```text
tree 48e8b04626764f736195aaed1e735ebc0f7a3a11
author Alice <alice@example.com> 1707104514 +0200
committer Alice <alice@example.com> 1707104514 +0200
>
Commit message

---
## 4. 🛡️ Права файлов в Git (File Modes)

> [!NOTE] Концепция хранения прав
> В Git каждый объект хранится в `.git/objects`.
> У файлов есть права доступа, но их список **строго ограничен**. Git сохраняет только **функциональную суть** файла (исполняемый он или нет), отбрасывая специфичные для ОС права (например, владельца файла).

| Mode (Octal) | Тип файла | Описание |
| :--- | :--- | :--- |
| `040000` | 📂 **Directory** | Каталог (папка). Используется внутри объектов Tree. |
| `100644` | 📄 **Regular File** | Обычный неисполняемый файл (`rw-r--r--`). Стандарт для кода. |
| `100644` | 👥 **Group-writeable** | *Редкость:* Обычный файл с правами записи для группы. |
| `100755` | 🚀 **Executable** | Исполняемый файл. Для скриптов (`.sh`, `.py`) и бинарников. |
| `120000` | 🔗 **Symbolic Link** | Символическая ссылка. |
| `160000` | 📦 **Git Link** | Ссылка на подмодуль (Submodule). |

---
## 🔗 Связи
* **Предыдущая тема:** [[Git - 00 Objects Type|Objects Type]]

