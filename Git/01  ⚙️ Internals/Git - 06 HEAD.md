---
tags:
  - 🐙git/theory
  - 🐙git
💠Stack: 🐙 Git
🌍 Scope:
  - 🌎 Global
🛡️ Risk: ✅ Safe / ⚠️ Moderate (Detached HEAD)
🏹 Target Area: References 📍
📝 Description: Механика веток и указателя HEAD как основы параллельной разработки.
---
# 🌿 Ветки и указатель HEAD

> [!ABSTRACT] Суть
> **Ветка** - это указатель на один из коммитов в истории репозитория. Ветка представляет собой логическое разделение работы, позволяя вести разработку параллельно или независимо.
> 
> **HEAD** - указатель на текущую ветку или коммит. Он показывает, где вы находитесь в дереве репозитория в данный момент. 
> - Индикатор текущей позиции: Git использует HEAD для понимания, какую ветку вы редактируете и куда будут добавляться новые коммиты.

---
## 📍 1. Ветка: Подвижный указатель
> [!SETTINGS] **Физическое воплощение**
> Каждая ветка — это файл в `.git/refs/heads/<name>`.
> Внутри этого файла лежит ровно одна строка: **SHA-1 хеш** коммита, который считается «верхушкой» (TIP) этой ветки.
> 
> $$Branch \rightarrow Commit\_Hash$$
### Как ветка «растет»?
Когда ты находишься на ветке и делаешь новый коммит:
1. Создается новый объект коммита.
2. Его родителем (`parent`) становится тот коммит, на котором ты стоял секунду назад.
3. **Файл ветки перезаписывается**: старый хеш стирается, записывается хеш нового коммита.
Ветка «двигается» автоматически.

---

## 🎯 2. HEAD: «Вы находитесь здесь»
Если веток может быть много, то **HEAD** в каждый момент времени (обычно) один. 
> [!INFO] **Иерархия указателей**
> В стандартном состоянии HEAD работает как «указатель на указатель»:
> $$HEAD \rightarrow \text{branch (main)} \rightarrow \text{Commit (7c43734)}$$
> 
> * **Файл:** `.git/HEAD`
> * **Содержимое:** `ref: refs/heads/main`

---
## ⚠️ 3. Состояние Detached HEAD (Отсоединенный HEAD)

Это происходит, когда ты переключаешься (`checkout`) не на имя ветки, а на **конкретный хеш коммита**.

> [!WARNING] **Что это значит?**
> **HEAD** теперь указывает напрямую на коммит, минуя «наклейку» (ветку). 
> $$HEAD \rightarrow \text{Commit (7c43734)}$$
> 
> **В чем опасность?**
> Если ты сделаешь коммит в этом состоянии, он не будет принадлежать ни одной ветке. Как только ты переключишься на `main`, найти этот «повисший» коммит будет сложно, и со временем его удалит сборщик мусора (Git Garbage Collector).

---
## 📊 4. Типы веток: Сравнение

| Тип                 | Где живет            | Префикс пути    | Описание                                                                                 |
| :------------------ | :------------------- | :-------------- | :--------------------------------------------------------------------------------------- |
| **Local**           | У тебя на диске      | `refs/heads/`   | Твое личное рабочее пространство.                                                        |
| **Remote-tracking** | У тебя, но как копия | `refs/remotes/` | Локальные закладки, показывающие, где были ветки на сервере в момент последнего `fetch`. |
| **Remote**          | На сервере (GitHub)  | —               | Настоящие ветки в облаке.                                                                |

---
## 🔗 Связи

> [!LINK] **Взаимодействие компонентов**
> * **Переключение:** Команды `git checkout` или `git switch` меняют содержимое файла `.git/HEAD`.
> * **Создание:** Команда `git branch <name>` создает новый файл в `refs/heads/`.
> * **Низкий уровень (Plumbing):**
> 	- `git symbolic-ref HEAD` — посмотреть/изменить, на что указывает HEAD.
> 	- `git update-ref` — «ручное» перемещение указателя ветки.
> * **Объекты:** Ветка всегда указывает на [[Git - 04 Commit |Commit]].