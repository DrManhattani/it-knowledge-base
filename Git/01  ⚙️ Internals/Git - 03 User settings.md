---
tags:
  - 🐙git/theory
  - 🐙git
💠Stack: 🐙 Git
🌍 Scope:
  - 🌐 System-wide
  - 🌎 Global
  - 📁 Local
🛡️ Risk: ✅ Safe
🏹 Target Area: Configuration ⚙️
📝 Description: Уровни и механизмы настройки Git, определяющие личность автора и поведение рабочей среды.
💻 Command: git config
---
# ⚙️ Конфигурация Git: Иерархия и Настройки

> [!ABSTRACT] Суть
> Настройки пользователя в Git — позволяют идентифицировать, кто выполняет коммиты и операции в репозитории. Эти настройки включают информацию об имени, электронной почте, а также другие параметры, такие как предпочтительный редактор или формат вывода команд.

---
## 🔝 1. Иерархия уровней (Hierarchy of Needs)
Git читает настройки в строго определенном порядке. **Правило простое:** более специфичный уровень всегда перезаписывает более общий ($Local > Global > System$).
> [!SETTINGS] Уровни конфигурации 🚩
> 
> 1. **SYSTEM** (`--system`)
>    - **Для кого:** Для всех пользователей ОС.
>    - **Файл:** `/etc/gitconfig` (в Windows: `C:\Program Files\Git\etc\gitconfig`).
>    - **Зачем:** Общие корпоративные политики или глобальные настройки редактора.
> 
> 2. **GLOBAL** (`--global`)
>    - **Для кого:** Для текущего пользователя системы.
>    - **Файл:** `~/.gitconfig` (или `%USERPROFILE%\.gitconfig`).
>    - **Зачем:** Твои личные данные (`user.name`, `user.email`), алиасы, цветовые темы. **Самый используемый уровень.**
> 
> 3. **LOCAL** (`--local`)
>    - **Для кого:** Только для конкретного репозитория.
>    - **Файл:** `.git/config` внутри проекта.
>    - **Зачем:** Настройка рабочей почты для корпоративного проекта, если в глобальных стоит личная.
> 
> 4. **WORKTREE** (`--worktree`) — *продвинутый уровень*
>    - **Зачем:** Настройки для конкретного рабочего дерева, если используешь `git worktree`. Позволяет иметь разные настройки внутри одного репозитория.

---
## 🛠️ 2. Tier-list основных команд:

> [!IMPORTANT] 🔴 Tier 1: Маст-хэв (Используется всегда)
> 
> Эти команды — база. Без них ты не настроишь рабочее место и не поймешь, почему Git ведет себя именно так.
> 
> - `git config --list` (или `-l`): Показать все активные настройки.
> - `git config <key>`: Быстро узнать значение конкретного параметра.
>     
>     - _Пример:_ `git config user.email` — покажет, какая почта сейчас «подхватится» для коммита.
>         
> - `git config --global user.name "Your Name"`: Установка личности. Кавычки обязательны, если в имени есть пробел.
> - `git config user.name` — узнать текущее значение (с учетом приоритетов).

> [!TIP] 🟠 Tier 2: Инспекция и аудит (Часто на практике)
> 
> Когда ты работаешь в нескольких проектах (например, личный на GitHub и рабочий в GitLab), возникают конфликты конфигов. Эти команды помогают распутать узлы.
> 
> - `git config --list --show-origin`: 🔍 **Самая полезная команда для дебага.** Она показывает не только настройку, но и путь к файлу, где она прописана. Сразу видно, перебивает ли `.git/config` (Local) твой основной `~/.gitconfig` (Global).
>     
> - `git config --list --show-scope`: Показывает уровень настройки (local, global, system) простым словом. Очень удобно для быстрой оценки.
>     
> - `git config --global --edit (или `-e`): Открывает глобальный файл конфига в твоем текстовом редакторе. Часто проще поправить всё руками в файле, чем вбивать 10 команд в консоли.
>     

> [!NOTE] 🟡 Tier 3: Обслуживание (Реже, но метко)
> 
> Команды для удаления или исправления ошибок в конфиге.
> 
> - `git config --unset <key>`: Удаляет конкретную настройку.
>     
>     - _Пример:_ Если ты случайно задал `user.name` на системном уровне и это тебе мешает — `git config --system --unset user.name`.
>         
> - `git config --get-regexp <term>`: Поиск настроек по части слова.
>     
>     - _Пример:_ `git config --get-regexp color` — покажет все настройки, связанные с цветом интерфейса.
>         

---
### ⚡2.1 Критически важные параметры (Nuances)

> [!INFO] 📄 Окончания строк: LF vs CRLF (`core.autocrlf`)
> 
> В разных операционных системах «конец строки» обозначается по-разному. Это невидимые символы, которые могут превратить твой код в кашу при совместной работе.
> 
> - **LF** (Line Feed, `\n`): Стандарт для Linux и macOS.
>     
> - **CRLF** (Carriage Return + Line Feed, `\r\n`): Стандарт для Windows.
>     
> 
> **Проблема:** Если один разработчик сохранит файл с CRLF, а другой с LF, Git может посчитать, что изменилась **каждая строка** в файле, хотя текст остался прежним.
> 
> **Решение:**
> 
> - `git config --global core.autocrlf true` (Для Windows): Git будет автоматически конвертировать LF в CRLF, когда ты скачиваешь код, и превращать обратно в LF, когда ты делаешь коммит. Это «золотой стандарт» для Windows-пользователей.
>     
> - `git config --global core.autocrlf input` (Для Linux/Mac): Git конвертирует CRLF в LF только при коммите.
>     

### 🎚️ 2.2 Как Git понимает типы данных?
> [!ABSTRACT] Типы значений в конфиге
> 
> 1. **Boolean (Логический)**: Принимает значения `true`/`false`, `yes`/`no`, `1`/`0`.
>     
>     - _Пример:_ `git config --global core.autocrlf true`
>         
> 2. **String (Строка)**: Любой текст.
>     
>     - **С кавычками**: Если есть пробелы или спецсимволы. `core.editor "code --wait"`.
>         
>     - **Без кавычек**: Если это одно слово. `core.editor vim`.
>         
> 3. **Path (Путь)**: Ссылка на файл или папку. В Windows часто используются прямые слеши `/` или экранированные обратные `\\`.
>     
>     - _Пример:_ `core.excludesfile ~/.gitignore_global`
>         

> [!EXAMPLE] ✒️ Редактор и кавычки: что мы передаем?
> 
> Когда ты настраиваешь редактор или другие параметры, ты передаешь **строковое значение** (String).
> 
> **Нужны ли кавычки?**
> 
> 4. **Если в значении есть пробелы** — кавычки **обязательны**.
>     
>     - Пример: `git config --global core.editor "code --wait"` (Здесь пробел между `code` и флагом, поэтому кавычки нужны).
>         
> 5. **Если значение — одно слово** — кавычки можно не ставить.
>     
>     - Пример: `git config --global core.editor vim` (Работает и так).
>         
> 
> **Как понять, что передавать?**
> 
> - **Boolean**: `true` / `false`.
>     
> - **String**: Текст, пути к файлам или команды терминала.
>     
> - **Integer**: Числа (например, для лимитов памяти).
>     
> 
> _Совет: Всегда ставь кавычки для путей и команд — это защитит тебя от ошибок интерпретации терминала._

---
### 🧠 Deep Dive / Question

> [!IMPORTANT] 🛠️ Как исправить автора, если `git config` не помог?
> 
> Если ты уже сделал коммиты с неправильной почтой, просто поменять конфиг недостаточно — Git «запекает» данные автора в хеш коммита навечно. Чтобы это исправить, нужно **переписать историю**.
> 
> 1. `git commit --amend --author="Name <email@example.com>"`
>     
>     - **Что делает:** Переоткрывает **самый последний** коммит, меняет в нем данные автора и создает новый объект коммита (старый удаляется).
>         
>     - **Когда юзать:** Если ошибся только что.
>         
> 2. `git rebase -i` **(Интерактивное перебазирование)**
>     
>     - **Что делает:** Позволяет «отмотать» историю на несколько шагов назад и пометить старые коммиты для редактирования (`edit`). Ты останавливаешься на каждом, меняешь автора через `--amend` и идешь дальше.
>         
>     - **Когда юзать:** Если ошибка тянется уже 5-10 коммитов.
>         
> 
> **Математика коммита:** >
> $$CommitID = \text{SHA-1}(Tree + Parent + Author + Message)$$
> _Если меняется Author, меняется и весь $CommitID$. Поэтому это считается «опасной» операцией для уже опубликованной истории._

> [!IMPORTANT] Что произойдет, если ты задал почту и в Global, и в Local?
> 1. Git запрашивает значение `user.email`.
> 2. Сначала он смотрит в `.git/config` (Local). Если там пусто...
> 3. Он идет в `~/.gitconfig` (Global). Если и там пусто...
> 4. Он идет в системный файл.
> 5. Если нигде ничего нет — Git выдаст ошибку при попытке коммита.

---
## 🔗 Связи
- **Исправление истории (если конфиг подвел):** [[Git - commit --amend]]
- **Особенности Windows:** [[Git - core.autocrlf]]
