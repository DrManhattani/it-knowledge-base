---
tags:
  - 🐙git
  - 🐙git/theory
💠Stack: 🐙 Git
🌍 Scope:
  - 🌎 Global
🛡️ Risk: ✅ Safe
📝 Description: Архитектура Git. Принципы работы DVCS, модель хранения данных (Snapshots vs Deltas) и жизненный цикл файлов.
⚡ Alias:
  - git architecture
  - dvcs theory
💻 Command: git --version
---
# 🐙 Глава: Архитектура и принципы Git
*Distributed Version Control System (DVCS)*

> [!ABSTRACT]+ 🎯 Определение
> **Git** — это распределенная система управления версиями (DVCS), предназначенная для отслеживания изменений в исходном коде, обеспечения целостности данных и поддержки нелинейных процессов разработки (ветвления).

---
## 1. 🛠️ Основные задачи

| Задача | Техническое описание |
| :--- | :--- |
| **Version Control** | Фиксация состояний проекта (Snapshots) с возможностью атомарного отката изменений и просмотра истории (Audit trail). |
| **Collaboration** | Обеспечение совместной разработки через слияние (Merge) независимых веток кода и разрешение конфликтов версий. |
| **Branching Model** | Поддержка изолированных контекстов разработки (Branches) для реализации фич (Feature-branches) без влияния на стабильную версию (Main/Master). |

---
## 2. 💎 Архитектурные особенности

### 2.1. Распределенная модель (Distributed)
В отличие от централизованных систем (CVCS, например, SVN), в Git каждый клиент имеет **полную локальную копию** репозитория, включая всю историю изменений.
* **Автономность:** Большинство операций (`commit`, `diff`, `log`, `branch`) выполняются локально без обращения к сети.
* **Отказоустойчивость:** Любой клон репозитория является полным бэкапом.

### 2.2. Модель хранения: Snapshots vs Deltas
Git мыслит не "различиями" (diffs), а **снимками** (snapshots).
* **Принципы:** При каждом коммите Git сохраняет слепок файловой системы.
* **Оптимизация:** Если файл не менялся, новый снимок содержит не копию файла, а ссылку (pointer) на предыдущую версию.

### 2.3. Целостность данных (Data Integrity)
Все данные в Git хешируются перед сохранением.
* **SHA-1:** Используется как уникальный идентификатор для каждого коммита, дерева или блоба.
* **Гарантия:** Невозможно изменить содержимое файла или историю коммитов без изменения соответствующего хеша (принцип цепочки блоков / DAG).

---

## 3. ⚔️ Git vs Hosting Services

Разграничение инструмента и сервиса.

| Характеристика | 🐙 Git (Tool) | ☁️ GitHub / GitLab (Service) |
| :--- | :--- | :--- |
| **Тип** | CLI-утилита, протокол. | SaaS/Self-hosted платформа хостинга. |
| **Функция** | Управление версиями, локальная работа. | Удаленное хранение, CI/CD, Code Review, Issues. |
| **Взаимодействие** | Локальный репозиторий (`.git`). | Remote Repository (Origin). |

---

## 4. 🧠 Workflow: Три состояния Git

Жизненный цикл изменений проходит через три логические зоны. Это фундаментальная концепция для понимания команд `add` и `commit`.

1.  **Working Directory (Рабочая директория):**
    * Текущее состояние файлов на диске (Sandbox). Здесь происходит редактирование кода.
2.  **Staging Area / Index (Индекс):**
    * Промежуточная зона. Файл-манифест, содержащий список изменений, которые попадут в следующий коммит (Pre-commit state).
3.  **Local Repository / HEAD (Репозиторий):**
    * База данных Git (`.git` directory). Здесь хранятся закоммиченные неизменяемые снимки (Snapshots).

---

## 5. 🗣️ Q&A для собеседования

> [!QUESTION]- В чем отличие Centralized VCS (SVN) от Distributed VCS (Git)?
> **Ответ:**
> * **Архитектура:** В SVN история хранится на центральном сервере, клиент получает только последнюю версию (Checkout). В Git каждый клиент имеет полный клон репозитория.
> * **SPOF (Single Point of Failure):** В SVN при отказе сервера работа встает. В Git разработчики могут продолжать коммитить локально.
> * **Сетевая зависимость:** Git требует сеть только для синхронизации (`push`/`pull`), SVN требует сеть для большинства операций.

> [!QUESTION]- Разница между `git pull` и `git fetch`?
> * `git fetch`: Обновляет ссылки на удаленные ветки (remote-tracking branches) в локальном репозитории, скачивая новые объекты, но **не изменяет** рабочую директорию. Безопасная операция для ревью изменений.
> * `git pull`: Комбинированная команда. Выполняет `git fetch`, а затем автоматически инициирует `git merge` (или `rebase`, если настроено) для слияния полученных изменений с текущей активной веткой.

> [!QUESTION]- Что такое HEAD?
> **HEAD** — это указатель (pointer/reference) на текущий коммит, с которым работает рабочая директория (Working Directory). Обычно HEAD указывает на имя активной ветки (например, `refs/heads/main`). Detached HEAD — состояние, когда указатель ссылается напрямую на коммит, а не на ветку.