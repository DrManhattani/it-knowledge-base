---
tags:
  - 🐙git/cmd
  - 🐙git
  - 🐙git/merge
💠Stack: 🐙 Git
📝 Description: Копирование изменений конкретного коммита и применение их в текущей ветке.
💻 Command: git cherry-pick
---
# 🍒 git cherry-pick

> [!ABSTRACT] **Определение (Interview-ready)**
> > **git cherry-pick** — это операция **дифференциального переноса**, которая вычисляет разницу (patch) между выбранным коммитом и его непосредственным родителем в исходной ветке и применяет этот патч к текущему `HEAD`.
>
> **Критический нюанс:** Эта команда игнорирует всю предыдущую историю ветки (все коммиты, идущие до выбранного). Она не «втягивает» состояние проекта, а воспроизводит только те строки кода, которые были изменены именно в этом конкретном коммите.
---

> [!SETTINGS] **Синтаксис и структура**
>
> ### 📘 Формальное описание (POSIX/Man-style)
>
> ```text
> git cherry-pick [<options>] <commit-ish>...
> git cherry-pick --continue | --skip | --abort | --quit
> ```
> 
> ### 📋 Детальный разбор параметров
>
> | Элемент | Техническое описание | Роль в операции |
> | :--- | :--- | :--- |
> | `<commit-ish>...` | **Целевые коммиты** | Хеши коммитов или имена веток. Можно передать список через пробел или диапазон (напр. `A..B`). |
> | `--continue` | **Продолжение** | Выполняется после ручного разрешения конфликтов. |
> | `--abort` | **Отмена** | Возвращает ветку в состояние до начала операции. |

---
### 🚩 Ключевые флаги (Professional Options)

| Флаг   | Полное имя        | Техническое назначение                                                                                                                                             |
| :----- | :---------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `-n`   | `--no-commit`     | **Stage only.** Применяет изменения в индекс и рабочую директорию, но не создает коммит автоматически. Полезно для объединения нескольких "вишенок" в один коммит. |
| `-x`   | —                 | **Traceability.** Добавляет в сообщение коммита строку `(cherry picked from commit ...)`. Критически важно для отслеживания происхождения патча.                   |
| `-m 1` | `--parent-number` | **Merge-pick.** Позволяет забрать изменения из merge-коммита, указывая, относительно какого родителя (обычно 1) вычислять диф.                                     |
| `-s`   | `--signoff`       | Добавляет подпись автора (Signed-off-by) в конец сообщения коммита.                                                                                                |
### 📋 Полная таблица флагов git cherry-pick

|**Флаг**|**Полное имя**|**Техническое назначение**|
|---|---|---|
|`-n`|`--no-commit`|**Stage only.** Применяет изменения в индекс и рабочую директорию, но не создает коммит автоматически. Полезно для объединения нескольких «вишенок» в один коммит.|
|`-x`|—|**Traceability.** Добавляет в сообщение коммита строку `(cherry picked from commit ...)`. Критически важно для отслеживания происхождения патча.|
|`-m 1`|`--parent-number`|**Merge-pick.** Позволяет забрать изменения из merge-коммита, указывая, относительно какого родителя (обычно 1) вычислять диф.|
|`-s`|`--signoff`|**DCO Sign.** Добавляет подпись автора `Signed-off-by` в конец сообщения коммита для подтверждения авторства.|
|`-e`|`--edit`|**Message Edit.** Открывает текстовый редактор перед созданием коммита, позволяя изменить сообщение «на лету».|
|`-S`|`--gpg-sign`|**Security.** Подписывает созданный коммит вашим GPG-ключом для подтверждения подлинности.|
|`--ff`|—|**Fast-forward.** Если текущий `HEAD` является родителем выбираемого коммита, Гит просто передвинет указатель вперед без создания нового объекта.|
|`--allow-empty`|—|**Empty Permit.** Позволяет создать коммит, даже если он не вносит никаких изменений (например, если код уже совпадает с текущим состоянием).|
|`--allow-empty-message`|—|**No-log Permit.** Позволяет сделать cherry-pick коммита, у которого пустое описание (в обычном режиме Гит выдаст ошибку).|
|`--keep-redundant-commits`|—|**Redundancy.** Сохраняет коммит, даже если он становится пустым из-за того, что его изменения уже присутствуют в истории ветки.|
|`-X`|`--strategy-option`|**Conflict Strategy.** Передает настройки стратегии слияния. Например, `-X theirs` автоматически выберет версию из «вишенки» при конфликте.|
|`--cleanup`|—|**Format Clean.** Определяет, как обрезать лишние пробелы и комментарии из сообщения коммита (`strip`, `whitespace`, `verbatim`).|
### 🚦 Флаги управления процессом (Sequencing)

Эти флаги используются, когда вы переносите пачку коммитов или столкнулись с конфликтом:

|**Флаг**|**Назначение**|
|---|---|
|`--continue`|**Продолжение.** Используется после ручного разрешения конфликтов и выполнения `git add`. Гит создаст коммит и перейдет к следующему в списке.|
|`--abort`|**Полная отмена.** Возвращает ветку в состояние «до начала команды» и чистит временные файлы.|
|`--skip`|**Пропуск.** Позволяет пропустить текущий проблемный коммит и перейти к следующему в диапазоне.|
|`--quit`|**Остановка.** Прекращает операцию, но оставляет `HEAD` и рабочую директорию там, где они застряли (в отличие от `abort`).|

---
### 🏗️ Внутренняя логика (How it works)

1. **Diff Generation:** Git вычисляет разницу (diff) между выбранным коммитом и его непосредственным родителем.
2. **Patch Application:** Полученный патч накладывается на текущий `HEAD`.
3. **Commit Creation:** Создается новый объект коммита с тем же автором и сообщением (если не указано иное), но с **новым SHA-1 хешем** и текущим временем.
4. **Conflict Check:** Если изменения в патче пересекаются с текущим состоянием файлов — Git останавливается и просит разрешить конфликт.

---
### 🛠️ Практические сценарии

> [!EXAMPLE] **Hotfix: Перенос исправления в Stable**
> Ты нашел баг в `develop`, исправил его, и теперь это же исправление нужно срочно доставить в `master`, не вливая весь `develop`.
> ```bash
> git checkout master
> git cherry-pick <hash-of-fix-from-develop>
> ```

> [!EXAMPLE] **Воскрешение работы из удаленной ветки**
> Если ветка была неудачной, но один коммит в ней был гениальным:
> ```bash
> git cherry-pick <commit-hash>
> ```

> [!EXAMPLE] **Сборка нескольких коммитов в один (с** `-n`**)**
> ```bash
> git cherry-pick -n <hash1>
> git cherry-pick -n <hash2>
> # Теперь оба изменения в индексе, можно сделать один коммит
> git commit -m "Merged features from two separate commits"
> ```

---
### ⚠️ Риски и нюансы

* **Дублирование (Duplicate Commits):** Постоянное использование `cherry-pick` вместо `merge` приводит к тому, что в истории появляются одинаковые по смыслу, но разные по хешам коммиты. Это может запутать `git merge` в будущем.
* **Потеря контекста:** Коммит может не заработать в новой ветке, если он зависит от кода, который не был перенесен.
* **Конфликты:** Если целевой файл сильно отличается от того, на котором делался оригинальный коммит.

---
### 🔗 Связи
- **Групповой аналог:** [[Git - git rebase]] (перенос всей цепочки)
- **Альтернатива:** [[Git - git merge]] (полное объединение веток)
- **Инспекция:** [[Git - git log]] (для поиска нужного хеша)

# 📦 Перенос группы коммитов (Batch & Range Cherry-pick)

> [!INFO] **Массовый перенос и диапазоны**
> 
> Если тебе нужно перенести несколько конкретных коммитов, которые не идут друг за другом в истории, ты просто перечисляешь их хеши пробел.
> #### 1. Перечисление (Batch)
> 
> Ты можешь указать произвольный список хешей через пробел. Git будет применять их по очереди в том порядке, в котором они записаны.
> 
> - **Команда:** `git cherry-pick <hash1> <hash2> <hash3>`
>     
> - **Механика:** Для каждого хеша создается отдельный новый коммит. Если на втором коммите возникнет конфликт, процесс остановится, и после его решения тебе нужно будет ввести `git cherry-pick --continue`.
>     
> 
> #### 2. Диапазоны (Ranges)
> 
> Когда коммиты идут в истории друг за другом, удобнее использовать синтаксис диапазона:
> 
> |**Синтаксис**|**Что попадет в выборку**|**Технический нюанс**|
> |---|---|---|
> |`A..B`|Все коммиты от A до B|**Исключая A**. Стандартная логика интервалов в Git.|
> |`A^..B`|Все коммиты от A до B|**Включая A**. Символ `^` указывает на родителя A.|
> 
> #### 3. Полезные модификаторы
> 
> - `git cherry-pick -n <range>` (`--no-commit`): Применяет все изменения из пачки коммитов к твоему рабочему дереву и индексу, но **не создает коммиты**. Это позволяет «схлопнуть» чужую серию правок в один твой коммит.
>     
> - `git cherry-pick -m 1 <merge_hash>`: Позволяет перенести мердж-коммит. Флаг `-m 1` указывает Гиту считать «главной» линией ту ветку, в которую вливали изменения.
>     
> 
> #### 4. Прерывание и продолжение
> 
> Если в середине пачки возник конфликт:
> 
> - `git cherry-pick --continue` — продолжить после решения конфликта и выполнения `git add`.
>     
> - `git cherry-pick --abort` — полностью отменить всю серию переносов и вернуться к состоянию до команды.
>     