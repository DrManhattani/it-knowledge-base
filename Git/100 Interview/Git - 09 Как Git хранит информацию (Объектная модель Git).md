---
tags:
  - 🐙git
  - 🐙git/questions
❔ Question: 09 Как Git хранит информацию? (Объектная модель Git)
🔧 Level: 🛡️ Basic
---
# Как Git хранит информацию? (Внутри .git/objects)

> [!ABSTRACT] **Главная идея**
> 
> Git — это простая база данных «ключ-значение». Ключом является **SHA-1 хеш** контента, а значением — сам контент. Эта система делает данные в Git **неизменяемыми (immutable)**: вы не меняете файл, вы создаете новый объект с новым хешем.

---
### 📦 Четыре типа объектов в Git

Вся история вашего проекта в папке `.git/objects` состоит всего из четырех типов кирпичиков:

|**Объект**|**Что хранит?**|**Аналогия**|
|---|---|---|
|**Blob** (blob)|Только **содержимое** файла.|Текст внутри документа (без названия).|
|**Tree** (tree)|Список имен файлов и ссылок на их Blobs или другие Trees.|Папка (каталог), содержащая файлы и подпапки.|
|**Commit** (commit)|Ссылка на корневое Tree, данные автора, дату, сообщение и ссылку на родителя.|Снимок всей системы в конкретный момент.|
|**Tag** (tag)|Ссылка на конкретный коммит с человекочитаемым именем (версией).|Закладка в книге.|

---
### 🛡️ Хеширование и Имена объектов

Git использует алгоритм **SHA-1** для создания 40-символьного идентификатора для каждого объекта.

- **Blobs:** Хеш зависит только от содержимого. Если в проекте 10 одинаковых картинок в разных папках, Git сохранит только **один** Blob.
- **Trees:** Хеш изменится, если изменится имя файла внутри или хеш любого объекта внутри этой «папки».
- **Commits:** Хеш включает в себя всё: дерево проекта, автора и хеш предыдущего коммита. Это создает неразрывную цепочку.

---
### 🔄 Механизм записи (Как это работает на практике)

Когда вы вносите изменения и делаете коммит, происходит цепная реакция:

1. **Создание Blob:** Вы изменили файл `app.py`. При `git add` Git создает новый Blob с новым контентом.  
2. **Обновление Tree:** При `git commit` Git создает новый объект Tree, который указывает на новый Blob для `app.py` и на старые Blobs для неизмененных файлов.
3. **Создание Commit:** Создается объект Commit, который «смотрит» на новое Tree.
4. **Движение указателя:** Ваша текущая ветка (например, `main`) просто переписывает внутри себя хеш на этот новый Commit.

> [!IMPORTANT] **Принцип неизменяемости**
> 
> Объекты в базе данных Git никогда не редактируются. Если вы изменили одну запятую, Git не меняет старый Blob, а записывает рядом новый. Старые объекты со временем могут быть удалены «сборщиком мусора» (git gc), если на них больше никто не ссылается.

---
### 🎙️ Как отвечать на собеседовании?

> [!note] «Git — это контентно-адресуемое хранилище.
> Это значит, что он находит данные не по имени файла, а по хешу его содержимого.
> 
> Вся структура строится на четырех объектах: **Blobs** хранят данные, **Trees** связывают данные с именами файлов и строят структуру папок, **Commits** связывают структуру папок с автором и историей (родителями), а **Tags** маркируют важные точки.
> 
> Такая архитектура гарантирует **целостность данных**: если в истории изменится хотя бы один бит, это изменит хеш объекта, что «сломает» все последующие хеши в цепочке, и Git мгновенно обнаружит подмену».