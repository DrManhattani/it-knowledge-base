---
tags:
  - 🐙git
  - 🐙git/questions
❔ Question: 01 Что такое Git
🔧 Level: 🛡️ Basic
---
# Что такое Git

> [!ABSTRACT] **Определение**
> **Git** — это распределенная система контроля версий,  предназначенная для отслеживания изменений в исходном коде.
> - Позволяет **сохранять историю изменений** проекта и при необходимости по ней **перемещаться**.
> - Позволяет нескольким разработчикам вести **параллельную разработку** благодаря наличию **веток**.
> - Вести разработку **локально** на своем устройстве и **синхронизировать изменения** через **удаленный** репозиторий.
> - Предоставляет механизмы для **обмена** кодом и **разрешения конфликтов** при операциях **слияния**.

---

>[!caution] 🎯 Зачем нужен Git? (Основные возможности)
>1. **Контроль через сохранение истории изменений** с помощью *коммитов*
   Git фиксирует состояние проекта в виде коммитов. Это позволяет в любой момент увидеть, кто, когда и какие именно строки кода изменил. Если в коде появилась ошибка, можно мгновенно откатиться к стабильной версии.
> 2. **Организация параллельной разработки** с помощью *веток*
> Благодаря механизму **веток (branches)**, несколько разработчиков могут одновременно работать над разными фичами в одном проекте, не мешая друг другу. Изменения в экспериментальной ветке не затронут основной код, пока они не будут проверены и объединены (merge).
>3. **Локальная работа** и синхронизация через *удаленный репозиторий*
   Git — распределенная система. Это значит, что вся история проекта хранится у тебя на компьютере. Ты можешь делать коммиты и работать с ветками без интернета, а затем синхронизировать свои изменения с **удаленным репозиторием** (GitHub, GitLab), когда появится доступ к сети.
> 4. **Обмен кодом** и *разрешение конфликтов* при слиянии
   Git предоставляет механизмы для обмена кодом и разрешения **конфликтов**, которые возникают, когда два человека одновременно редактируют один и тот же участок файла.

### 💡 Резюме для интервьюера
«Git — это стандарт индустрии, который решает три главные задачи: **сохранение истории проекта**, **возможность безопасной параллельной работы через ветки** и эффективную **синхронизацию кода** между **локальными** машинами разработчиков и **удаленным** сервером».