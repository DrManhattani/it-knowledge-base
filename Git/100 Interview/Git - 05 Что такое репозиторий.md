---
tags:
  - 🐙git
  - 🐙git/questions
🔧 Level: 🛡️ Basic
❔ Question: "05 Определение репозитория: Рабочее дерево vs Каталог .git. Структура и ключевые особенности."
---
# Что такое репозиторий?

> [!ABSTRACT] **Определение**
> **Репозиторий Git** 
> - **в широком смысле**, репозиторий — это вся папка вашего проекта, которая была проинициализирована через команду `git init`. Это пространство, где Git следит за файлами. Оно состоит из двух частей: **Рабочего дерева** и скрытого **каталога** `.git`.
> - **в строгом техническом смысле**, репозиторий — это именно содержимое папки `.git`. Это локальная база данных, где хранятся все сжатые снимки состояния (snapshots), история коммитов и указатели на ветки.

---
### 🏗️ Анатомия репозитория
Любой локальный репозиторий Git состоит из двух физических частей:

1. **Рабочее дерево (Working Tree)** — это видимый каталог, где лежат твои файлы. Здесь ты их редактируешь, создаешь и удаляешь. В нем могут находиться как **отслеживаемые** файлы (уже известные Git), так и **неотслеживаемые** (новые файлы, которые еще не были добавлены).
2. **Каталог `.git`** — скрытая папка в корне проекта. Это «сердце» и база данных репозитория. Если её удалить, проект превратится в обычную папку, а вся история правок исчезнет.

---
### 📂 Что прячется внутри папки `.git`? (Middle Insight)
Интервьюеры любят спрашивать, что именно Git хранит в этой скрытой папке:
* **Object Database (База объектов):** Хранилище всех коммитов, деревьев (структур папок) и блобов (содержимого файлов).
* **Refs (Ссылки):** Файлы-указатели на конкретные коммиты (твои ветки и теги).
* **Config:** Настройки конкретно этого репозитория (например, URL удаленного сервера или данные автора).
* **Index:** Файл, описывающий «промежуточную область» (Staging Area).

---
### ⚔️ Ключевые особенности (Почему это эффективно)

Когда мы говорим о рабочем процессе, важно понимать, что репозиторий — это **третий этап** жизненного цикла изменений. У нас есть:

1. **Рабочая область** (Working Directory) — где мы просто редактируем файлы.
2. **Область подготовки** (Staging Area) — где мы формируем список того, что пойдет в коммит.
3. И, собственно, **Репозиторий** — куда данные попадают после команды `commit` и сохраняются там навсегда.

**Почему это важно?** Потому что если вы удалите все файлы в рабочей папке, кроме каталога `.git`, вы ничего не потеряете — вы сможете восстановить любую версию проекта из репозитория-базы. Но если вы удалите папку `.git`, то ваш проект превратится в обычную папку, а история версий исчезнет навсегда».

---
### 💡 Резюме для интервьюера
«Репозиторий — это совокупность рабочего каталога и скрытой базы данных в папке `.git`. Главное отличие репозитория Git от обычной папки в том, что каждый файл в нем контролируется по целостности, а каждое изменение зафиксировано в истории, которая распределена между всеми участниками команды».