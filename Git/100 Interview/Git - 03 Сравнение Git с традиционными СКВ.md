---
tags:
  - 🐙git
  - 🐙git/questions
❔ Question: "03 Сравнение Git с традиционными СКВ (SVN, CVS). Архитектурные преимущества: Snapshots, Integrity, Index."
🔧 Level: 🛡️ Basic
---
# 🧠 Git vs SVN / CVS — технические отличия (уровень собеседования)

> [!ABSTRACT] **Суть различия**  
> Git — это не «улучшенный SVN», а переход от централизованной модели хранения файлов к **распределённой системе управления графом неизменяемых объектов**, где история представлена как криптографически связанный DAG.

---
## 📦 1. Модель данных: **Snapshots vs File Revisions**

> [!INFO] **Как Git мыслит историю**  
> Git логически хранит проект как последовательность **снимков состояния** (snapshots) файловой системы, а не как цепочку правок к одному файлу.

|SVN / CVS|Git|
|---|---|
|История основана на версиях файлов|История основана на снимках всего дерева|
|Фокус на «изменениях файла»|Фокус на «состоянии проекта»|
|Ветка — namespace-копия|Ветка — указатель на commit|

> [!NOTE]  
> Физически Git **тоже использует дельта-сжатие** внутри pack-файлов. Разница не в способе хранения байтов, а в **модели представления истории**.

---
## 🔐 2. Криптографическая модель целостности

> [!IMPORTANT] **Git = Merkle DAG**

Каждый объект в Git:
- blob (файл)
- tree (каталог)
- commit
адресуется по хешу содержимого.

Коммит содержит:

```
tree + parent commit hash + metadata
```

Это образует **Merkle Directed Acyclic Graph**.

> [!SUCCESS]  
> Любое изменение в истории меняет хеши всех последующих коммитов → изменения невозможно внести «незаметно».

> [!WARNING]  
> Git не запрещает переписывать историю (`rebase`, `filter-repo`), но делает это **криптографически обнаружимым**.

---

## 🗂️ 3. Индекс (Staging Area)

> [!INFO]  
> Git добавляет промежуточный слой между рабочей директорией и коммитом.

```
Working Directory → Index → Commit
```

Это позволяет:

- собирать атомарные изменения
- частично добавлять файлы
- формировать логические коммиты

> [!TIP]  
> В централизованных системах commit = «всё, что изменено». В Git commit = «то, что ты явно выбрал».

---

## 🌿 4. Ветки и структура истории

> [!IMPORTANT] **Ветка в Git — это просто ref**

```
refs/heads/main → SHA-commit
```

Операции ветвления:

|SVN|Git|
|---|---|
|Серверная операция|Локальная операция|
|Namespace-копия|Перемещение указателя|
|Ветка как структура|Ветка как ссылка|

> [!SUCCESS]  
> Стоимость создания ветки ≈ запись нового SHA в файл.

---

## 🌍 5. Централизованная vs распределённая модель

> [!ABSTRACT]  
> SVN — клиент/сервер  
> Git — peer-to-peer система с optional server

|SVN|Git|
|---|---|
|История на сервере|Полная история у каждого|
|Большинство операций требуют сеть|Большинство операций локальные|
|Сервер — критическая точка|Сервер — точка синхронизации|

> [!NOTE]  
> Это снижает риск потери данных, но не гарантирует защиту без резервных копий.

---

## ⚡ 6. Производительность

> [!INFO]  
> Git быстрее не просто «потому что локально», а из-за архитектуры:

- content-addressable storage
- immutable objects
- граф истории вместо линейной базы
- cheap refs вместо копий

---

## 🧩 7. Ключевая разница философии

> [!IMPORTANT]  
> SVN управляет **файлами**.  
> Git управляет **графом состояний проекта**.

---

# 🎯 Формулировка для интервью

> [!SUCCESS]  
> Git — это распределённая система управления версиями, построенная как content-addressable база неизменяемых объектов. История проекта представлена как Merkle DAG коммитов. Ветки являются ссылками на коммиты, что делает ветвление и слияние дешёвыми операциями. Git логически оперирует снимками состояния, хотя физическое хранение оптимизируется дельта-сжатием. Локальность операций и неизменяемость объектов обеспечивают высокую производительность и надёжность.