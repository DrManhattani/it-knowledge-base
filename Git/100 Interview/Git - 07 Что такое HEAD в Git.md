---
tags:
  - 🐙git
  - 🐙git/questions
❔ Question: 07 Что такое HEAD в Git?
🔧 Level: 🛡️ Basic
---
# Что такое HEAD в Git?

> [!ABSTRACT] **Определение**
> 
> **HEAD** — это специальный указатель (ссылка) в Git, который определяет ваше текущее местоположение в репозитории. Проще говоря, это ответ на вопрос: «Где я сейчас нахожусь и какой коммит будет родителем для моего следующего изменения?»

---
### 🔍 Как работает HEAD

В 99% случаев HEAD работает по принципу «матрешки»:

1. **HEAD** указывает на **текущую ветку** (например, `main`).
2. **Ветка** указывает на **последний коммит** в этой ветке.

Когда вы делаете новый коммит, ветка «переезжает» вперед, а HEAD послушно следует за ней, оставаясь на острие разработки.

---
### 🧪 Состояние "Detached HEAD" (Отсоединенный HEAD)

На собеседовании обязательно спросят: «А может ли HEAD указывать не на ветку?».

**Да, это называется Detached HEAD.**

- **Когда это происходит:** Когда вы переключаетесь (checkout) не на ветку, а на конкретный **хеш коммита** или **тег**.
- **Суть:** Вы находитесь вне какой-либо ветки.
- **Опасность:** Если вы начнете делать коммиты в этом состоянии, они не будут принадлежать ни одной ветке. Как только вы переключитесь обратно на `main`, найти эти «повисшие в воздухе» коммиты будет сложно (придется лезть в `git reflog`).

---
### 🔩 Под капотом (Low-Level)

Физически HEAD — это обычный текстовый файл по адресу `.git/HEAD`.
- Если вы откроете его, когда находитесь на ветке `main`, вы увидите: `ref: refs/heads/main`.
- В состоянии Detached HEAD там будет просто 40-символьный **SHA-1 хеш** коммита.