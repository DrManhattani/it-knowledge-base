---
tags:
  - 🐙git
  - 🐙git/questions
❔ Question: 02 Ключевые понятия Git
🔧 Level: 🛡️ Basic
---
# 02 Ключевые понятия Git: Глоссарий

> [!danger] 📦 Коммит (Commit)
**Commit** — это объект, который **фиксирует** определённое **состояние репозитория**. Он содержит ссылку на объект дерева (tree), который отражает состояние репозитория на момент создания коммита, а также метаданные (автора, сообщение коммита, дату и ссылку на предыдущий коммит).
>>[!warning] Под капотом:
>>**1. Файловая система:** Неизменяемый файл в базе `.git/objects`.
>>**2. Состав:** 
>>* **Hash Tree:** Ссылка на объект «дерева», отражающий состояние репозитория на момент создания коммита.
>>* **Parent(s):** Хеш предыдущего коммита (у первого коммита его нет, у мерж-коммита — два и более). 
>>* **Metadata:** Автор (тот, кто придумал или сделал изменения), коммиттер (тот, кто создал коммит), метка времени и email. 
>>* **Message:** Текст описания.

> [!success] 🌳 Ветка (Branch)
**Branch** — это независимая линия разработки. Дополнительные ветки позволяют работать над проектом, изолировав его от остальной кодовой базы до тех пор, пока она не будет готова и объединена с основной ветвью.
>>[!warning] Под капотом:
>>**1. Файловая система:** текстовый файл в папке `.git/refs/heads/<name>`
>>**2. Внутри файла:** 40-символьный SHA-1 хеш коммита, на который ветка указывает в данный момент. Формально это просто **указатель на коммит**
>>**3. Механизм:** Когда ты делаешь новый коммит, находясь на ветке `main`, Git: 
>>1. Создает новый объект коммита. 
>>2. Переписывает хеш внутри файла `.git/refs/heads/main` на хеш этого нового коммита.
>>
>>*Master* — это ветка по умолчанию, которая есть во всех репозиториях Git. Ее можно переименовать через `git branch -m <old> <new>`
>
>>[!error] Не путать с **HEAD**:
>>**HEAD:** Это файл (`.git/HEAD`), **указатель**, который содержит ссылку на файл ветки или коммит (например, `ref: refs/heads/main`). Он показывает, где вы находитесь в дереве репозитория в данный момент.

> [!hint] 🧬 Слияние (Merge)
**Слияние** — это процесс объединения истории двух разных веток.
>>[!warning] Под капотом:
>> **Суть:** Git сравнивает три коммита: верхушку ветки main, верхушку ветки feature и их последнего общего предка.
>> - Если верхушка ветки совпадает с общим предком, то происходит слияние **Fast-forward Merge**. Т.е. простое передвижение указателя ветки вперед
>> - Если не совпадает, то происходит трестороннее слияние **3-Way Merge**. Git берет эти 3 коммита и сравнивает, какие изменения были внесены в каждой из веток. Если нет конфликтов при слиянии создается **Merge Commit** 
	
> [!info] 🌐 Удаленный репозиторий и Origin (Remote & Origin)
> * **Удаленный репозиторий (Remote):** Версия проекта, живущая на сервере (GitHub, GitLab, локальный сервер). Нужна для синхронизации работы команды.
>>[!warning] Под капотом:
>> **1. Файловая система**: Все данные об удаленных репозиториях хранятся в текстовом файле `.git/config`
>> 
>> *Origin* - это **псевдоним (alias)** для URL твоего основного удаленного репозитория. Это стандартное имя по умолчанию для удаленного репозитория. Можно переименовать через `git remote rename <old> <new> `
>>  * `origin` — это не само «хранилище объектов», а просто короткое имя, чтобы не писать каждый раз полный адрес `https://github.com/user/repo.git`.
>>  * `origin` автоматически создается командой `git clone`

---
### 💡 Резюме для интервьюера
«Коммит — это снимок в истории, сохранение состояния репозитория. Ветки — это способ вести параллельную работу. Слияние — способ собрать результат параллельной работы. А удаленные репозитории синхронизировать работу разработчиков».